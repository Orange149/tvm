# Accelerating MapReduce on a coupled CPU-GPU architecture（2012）
解决：cpu-gpu怎么协作
1. 公用缓冲区（zero-copy buffer）避免在runtime中反复搬运
2. map-dividing（任务切分）采用runtime调优方法来平衡每个调度单元的任务块大小
3. map阶段pipelining
在 CPU–FPGA 的共享 DDR 场景下，将 DNN 直接套用 MapReduce 并不合适，因为推理执行是具有严格依赖的算子 DAG，而非按 key 的大规模可交换归约。然而，Chen 等人在耦合 CPU–GPU 上提出的 zero-copy 共享缓冲区、任务块粒度调优与“边产生边归并（continuous reduction）”的思想，对 CPU–FPGA runtime 仍具有直接启发。需要特别区分：共享物理 DDR 只提供了零拷贝的可能性，并不会让 GraphExecutor/Pipeline Executor 自动停止 `set/get/QueueData` 的框架复制；真正的零拷贝还需要外部 buffer 绑定、跨 stage 生命周期和完成同步。
# Understanding the design space of dramoptimized hardware fft accelerators
重排数据布局，让 DRAM 连续访问最大化。
传统 FFT是stride access
即：A[0], A[64], A[128]...这样每隔stride个步长破坏DRAM row buffer locality造成大量activate/precharge，与后续做tile、chunk、buffer layout有关。
# Impact of cache architecture and interface on performance and area of FPGA-based processor/parallelaccelerator systems

Memory 层次结构里有三层带宽
Accelerator → Cache port → Cache SRAM → DDR controller → DDR banks

可能瓶颈在：

1. Cache port 数（最常见）---多accelerator少port

2. SRAM 频率     ---accelerator和SRAM的频率差距

3. DDR controller 好的 controller-可以 pipeline、re-order 、开多个 bank

4. DDR bank 冲突

论文研究的是第 1 层。
# DDR 结构
[DDR结构]
# Optimizing FPGA-based Accelerator Design for Deep Convolutional Neural Networks（2015）
在 2015 年之前，大多数 FPGA-based CNN 加速器设计主要侧重于提升计算并行度，例如增加 PE 数量或提高 DSP 利用率，而对外部存储系统的约束关注不足。然而，在实际平台中，DDR 带宽是有限资源，不合理的 tile 设计会显著增加 DRAM 访问次数，导致数据传输成为性能瓶颈，从而使计算单元因等待数据而空闲。

该论文的关键贡献在于，从内存访问的角度对 CNN 加速器设计进行了系统化建模。具体而言：

1. 作者对 DRAM 访问量进行了定量分析，推导出 DRAM 访问量与 tile 参数之间的解析关系，使得数据移动成本可以被显式计算，而非依赖经验估计。
    
2. 在此基础上，引入了 computation-to-communication (CTC) ratio，并结合 roofline 模型，将计算能力与内存带宽约束统一到同一分析框架中，从而可以评估不同设计在带宽约束下的可达性能。
    
3. 进一步地，论文系统枚举合法的 tile 参数空间，并基于带宽与计算匹配原则搜索最优设计点，将 tile 选择问题转化为一个受内存带宽约束的优化问题，而非单纯的计算资源最大化问题。

总体而言，该论文的重要意义在于，将 CNN 加速器的设计目标从“计算资源最大化”转变为“计算能力与内存带宽匹配”的协同优化问题。
# **Eyeriss (ISCA 2016)**
是一项面向卷积神经网络的专用硬件架构研究，其核心关注点并非软件调度或运行时管理，而是通过设计特定的数据流（Row-Stationary, RS）与多级片上存储结构（Register File、Global Buffer、PE Array）来最小化数据移动能耗。论文指出，在 CNN 推理中，计算（MAC）成本远低于数据移动成本，尤其是 DRAM 访问与部分和（partial sum, psum）的跨层级读写。因此，Eyeriss 将卷积计算中的数据复用形式系统化为三类：空间滑窗带来的 convolutional reuse、跨输出通道的 ifmap reuse，以及跨 batch 的 filter reuse，并建立了一个层级化能耗模型，将不同存储层级的访问次数与单位能耗显式建模，从而将数据流设计转化为“在多级存储体系下最大化低层级复用、最小化高代价访问”的优化问题。虽然该工作主要用于指导硬件结构设计（如 PE 互联方式、RF 容量配置、片上通信模式等），其提出的核心结论对软件 runtime 仍具有普适意义：卷积内存压力并非由 3×3 kernel 本身决定，而是由输出并行规模（空间 tile × Cout）、归约维度分块（Cin × R × S）以及 psum 生命周期共同决定；在实际系统中，psum 的溢出与回写往往是数据移动开销的关键来源。因此，从软件层面来看，Eyeriss 的数据流思想可以被转译为 runtime 的 buffer 驻留与 tile 决策问题：在既定硬件层级与带宽约束下，优先保证中间结果在低代价存储层完成归约，再在此约束下最大化输入与权重的复用，并通过合理的分块与调度减少跨层级数据移动。换言之，该工作虽然属于硬件架构范畴，但为后续软件级共享内存管理与异构调度提供了重要的分析框架与内存压力建模思路。
# **LCMM (DAC 2019)**

是一项面向 FPGA DNN 加速器片上存储分配优化的研究，其核心关注点并非异构调度或运行时系统设计，而是在**既定 FPGA 硬件结构（PE 阵列、tile buffer、DDR 带宽）约束下，通过编译期分析与形式化建模，优化片上 SRAM 的分配策略，从而缓解 memory-bound 层的性能瓶颈**。论文首先通过 roofline 模型对 DNN 各层进行定量分析，指出在现代网络结构（如 Inception、ResNet）中，大量层的可达性能受限于外部 DRAM 带宽，而非计算资源；因此统一的 tile-based 内存管理策略会导致片上存储利用率不足，并使 memory-bound 层成为整体性能瓶颈。在此基础上，LCMM 提出了三项关键机制：其一，对 feature tensor 进行全局生命周期（liveness）分析，构建 interference graph 并通过图着色实现跨层 buffer 复用；其二，对 weight tensor 进行预取时间窗建模，构建 prefetch dependence graph，在计算与搬运之间建立重叠关系；其三，将片上存储分配问题形式化为带“pivot compensation”的 knapsack 优化问题，通过动态规划在容量约束下选择能够最大化延迟降低的 virtual buffers。该方法本质上将“哪些 tensor 应驻留片上”这一问题转化为“在给定带宽与容量约束下，最大化 latency reduction”的全局优化问题。

虽然该工作主要用于指导 FPGA 加速器的编译期存储布局与电路资源分配，并未涉及 CPU–FPGA 共享内存 runtime 或动态调度机制，但其核心结论对系统层研究仍具有重要意义：第一，layer 是否值得优化必须通过 compute-to-communication 比建模，而非凭经验判断；第二，tensor 的生命周期决定了可复用性，复用应以“延迟收益”而非“出现频率”为决策依据；第三，memory-bound 层的性能改善依赖于对带宽压力与归约路径（尤其是部分结果写回）的显式建模。从软件与 runtime 的角度来看，LCMM 的思想可以被转译为：在既定硬件与带宽约束下，通过建模数据移动成本与生命周期信息，优先为关键 tensor 保证低代价存储路径，并通过分块、预取与调度策略减少高代价跨层级访问。换言之，该工作虽属于编译期片上存储优化范畴，但为后续 CPU–FPGA 共享内存 runtime 的内存压力建模、buffer 驻留策略与异构调度决策提供了重要的分析框架与定量方法论。
# 服务器端vs嵌入式端技术栈对比
| 抽象层      | 服务器 GPU                                | 嵌入式 CPU–FPGA（VTA）                      |
| -------- | -------------------------------------- | -------------------------------------- |
| 图层       | ONNX / Torch                           | ONNX / 手写子图                            |
| 编译层      | TVM/XLA/TRT → CUDA kernels             | TVM → VTA codegen                      |
| Runtime  | CUDA runtime（streams/events）           | VTA runtime / 你的 runtime（queue/event）  |
| Memory   | cudaMalloc + memory pool + unified mem | CMA/dma-buf + arena + flush/invalidate |
| Driver   | NVIDIA driver                          | DMA driver / uio / dma-proxy / XRT（可选） |
| Hardware | GPU + HBM/GDDR + PCIe/NVLink           | FPGA + DDR/BRAM + AXI + ACP/HP/HPC     |

# Efficient Memory Organization for DNN Hardware Accelerator Implementation on PSoC（2021）
![[Pasted image 20260223182247.png]]
BiMapTab 的核心不是“更快的 DMA”，而是“更合理的 DMA buffer 组织方式”。它用 Memory_Manager 在物理内存中预留并管理多个 mapping table，再用 Mem_Mapping_Tab 把每张表切分为多个同时具有物理地址与虚拟地址的 chunk。由于 DMA 传输只依赖 chunk 的物理地址与大小，任意 chunk 都可在运行时被用作 MM2S 或 S2MM，从而避免了原生驱动固定方向 buffer 带来的 PS 侧多余 memcpy，并且更便于按层组织 weights/bias 与 activations。
![[Pasted image 20260223182306.png]]
# ShortcutFusion: From Tensorflow to FPGA-Based Accelerator With a Reuse-Aware Memory Allocation for Shortcut Data（2022）

是一项面向 FPGA 加速器的硬件—编译协同优化研究，其核心关注点并非算子层面的计算重排或通用编译器自动调优，而是从“跨层数据生命周期与片上存储分配策略”的角度，系统性地减少残差网络中 shortcut 数据所引发的冗余片外访问开销。论文指出，在包含 residual / squeeze-and-excitation 等结构的现代 CNN 中，shortcut 分支的数据依赖显著拉长了中间 feature map 的生命周期，使得传统以单层为单位的数据流设计和固定重用策略难以避免频繁的 DRAM 往返访问，从而导致带宽压力与能耗增加。为此，ShortcutFusion 提出一种 block-wise 的重用模式切换策略，在 row-based weight reuse 与 frame-based weight reuse 之间进行静态分段选择，并结合 reuse-aware static memory allocation，对输入、输出与 shortcut 数据进行跨层生命周期分析与物理缓冲区分配，使得 residual block 内的 shortcut 数据能够驻留片上并通过数据前递直接参与融合计算，而无需回写至外部存储。与此同时，论文设计了支持共享 MAC 阵列与三物理缓冲区结构的 FPGA 加速器架构，使编译器生成的重用策略能够在硬件层得到直接映射。总体而言，ShortcutFusion 的重要意义在于，将“数据流优化”从单层 tile 级别扩展到 block 级别的数据生命周期管理问题，在既定片上 SRAM 约束下，通过静态分析与模式切换最小化片外访问。然而，该工作仍属于单加速器内部的静态优化范畴，并未涉及运行时级别的跨资源数据位置跟踪或一致性管理。其思想对 SoC 场景的启发在于：当数据依赖跨越多个算子时，单层最优的数据流设计未必能实现全局最优，必须引入生命周期感知的内存分配与策略切换机制。换言之，ShortcutFusion 并未改变算子本身的计算形式，而是通过精细化的跨层数据重用建模，将“shortcut 访问开销”从隐式结构代价转化为显式可优化对象，为后续面向共享内存异构系统的生命周期驱动型 runtime 设计提供了结构层面的借鉴。
![[Pasted image 20260225152225.png]]
# MoCA: Memory-Centric, Adaptive Execution for Multi-Tenant Deep Neural Networks（2023）
# Scratchpad Memory Management for Deep Learning Accelerators（ICPP 2024）
是一项面向深度学习加速器片上存储系统（scratchpad/GLB）的内存管理研究，其核心关注点并非计算单元设计或数据流架构创新，而是从“有限片上存储资源的动态分配与复用策略”的角度，系统性地减少 off-chip memory 访问与访存延迟。论文指出，在现代 DL 加速器中，性能与能效瓶颈往往不在计算阵列本身，而在于固定划分的片上 buffer 难以适应不同网络层之间高度异构的 ifmap/filter/ofmap 内存需求。传统 separate buffer 或半共享 buffer 设计虽然硬件简单，但在层间特征尺寸变化时会产生空间浪费，进而导致不必要的 DDR 访问。为此，该工作提出以统一的 Global Buffer（GLB）为基础，通过软件层面的 memory management technique 动态决定每一层的 scratchpad 使用策略。论文设计了多种 reuse policy（ifmap reuse、filter reuse、per-channel reuse 及其部分变体），并基于轻量级估算模型对 memory footprint、off-chip access 以及 latency 进行分析，从而为每一层选择最优策略，形成 heterogeneous execution plan。不同于仅追求访存最小化的静态 tiling 方法，该方法还显式引入 prefetch 空间建模，在 reuse 与 latency overlap 之间进行权衡，使片上空间既可用于最大化数据复用，也可用于隐藏访存延迟。总体而言，该工作的意义在于，将 scratchpad 设计从“固定结构优化问题”转化为“策略可切换的层级内存规划问题”，使 buffer 利用率成为可优化对象。虽然该研究仍局限于加速器内部 memory subsystem 层面，并未涉及跨设备数据一致性或异构调度，但其核心思想对更高层 runtime 设计具有启发意义：即在共享资源受限条件下，通过显式建模数据驻留策略与生命周期阶段，将“片上空间分配”从静态硬件约束转化为可决策的策略空间，从而为后续 CPU–FPGA 共享内存 runtime 中的 buffer 管理、tile 搜索与 overlap 策略设计提供抽象基础。
![[Pasted image 20260225151055.png]]

# **RIMMS (2025)**

是一项面向动态异构系统（CPU–GPU/CPU–FPGA）的运行时内存管理研究，其核心关注点并非加速器内部的数据流设计或片上结构优化，而是从“数据对象生命周期与跨资源位置管理”的角度，系统性地减少运行时中的冗余数据迁移与内存分配开销。论文指出，在异构计算环境中，性能瓶颈往往并不来自算子计算本身，而来自保守的数据一致性策略与重复的资源间同步：在缺乏精确数据位置跟踪的情况下，运行时通常在每次 API 调用后强制同步回 host，或在资源切换前无条件迁移数据，从而产生大量不必要的 copy 或同步操作。为此，RIMMS 提出了 `hete_Data` 抽象，将每个数据对象建模为跨资源共享的逻辑实体，并通过 last-resource/location tracking 显式记录哪一个资源指针持有有效数据，使“是否需要迁移”从保守策略转化为可判定问题；仅当跨资源访问真正发生时才触发数据移动。进一步地，论文提出 fragment 机制和 bitset/linked-list/NF 等 marking 方案，在分配时间、元数据和内存利用率之间权衡。RIMMS 并未提出本项目所需的共享 DDR contention 模型，该问题仍应由 P7C 的 matched experiment 单独判断。

RIMMS 的 task-to-PE 映射在运行时才确定，因此必须处理多个 memory/PE 上的数据位置和迁移；本项目的 CPU/VTA 切图在运行前固定，并使用同一片共享 DDR，不需要照搬完整的动态 location manager。这里可迁移的是“显式记录状态、只做必要动作”的原则：把每个边界张量表示为预分配 slot，并记录 owner、frame generation、consumer count 和 completion。HPC/coherent 主线已由硬件维护 cache coherence，不应为了套用 RIMMS 而重新加入 flush/invalidate；剩余问题是消除 TVM 框架复制，并保证同一 slot 不会被上一帧和下一帧同时覆盖。

## TVM Pipeline Executor 与 slot zero-copy 的边界

TVM Pipeline Executor 已经提供 stage DAG、worker thread、SPSC queue 和 notification，解决“什么时候运行哪个 stage”。但当前实现的 `QueueData` 在 queue push/poll 时深拷贝 tensor，consumer 又调用普通 `SetInput` 复制到自己的输入。因此它的控制流水线可以复用，数据通路却不是跨 CPU/VTA Executor 的共享物理 slot。

本项目不把 TVM 已有的 `set_input_zero_copy/set_output_zero_copy` 当成创新。要补的是 API 上层的运行时协议：由 manifest 判定 edge 是否兼容；从 u-dma-buf 预分配两个 slot；producer/consumer 用同一地址的 CPU/VTA `DLTensor` view；queue 只传 `{edge_id, slot_id, generation}`；`run()` 完成后再释放 slot。双 slot 用于让 producer 准备下一帧时 consumer 使用上一帧，状态机用于避免旧帧、覆盖和悬空指针。
![[Pasted image 20260225151129.png]]

# TVM – Unified Static Memory Planner (USMP)
- **链接（讨论/实现入口）**：
    
    - RFC / 设计说明：[https://discuss.tvm.apache.org/t/rfc-unified-static-memory-planning/10099](https://discuss.tvm.apache.org/t/rfc-unified-static-memory-planning/10099?utm_source=chatgpt.com)
        
    - 代码实现 trace：[https://github.com/apache/tvm/issues/8404](https://github.com/apache/tvm/issues/8404?utm_source=chatgpt.com)
        
- **核心内容**：USMP 在 TVM 编译阶段对所有 tensor 做 holistic memory planning（考虑内/算子内外 tensor）。
    
- **重点**：TVM 的 static memory planner 目标是**消除 backend malloc/free 调用**并把所有可静态分配的 tensor 映射到一个单一 buffer + offset
# Static Memory Planning Tutorial / Survey
[ML图的静态内存规划](https://apxml.com/zh/courses/compiler-runtime-optimization-ml/chapter-3-advanced-graph-level-optimizations/static-memory-planning)

# Analysis and Optimization of I/O Cache Coherency  Strategies for SoC-FPGA Device
该工作围绕 SoC-FPGA 平台（以 Xilinx Zynq UltraScale+ 为代表）中的 I/O cache coherency 机制展开系统性分析，其研究重点并非加速器内部算子结构或片上数据流优化，而是从“CPU–FPGA 共享内存一致性维护”的角度出发，定量评估不同 coherency 策略在实际系统中的软硬件开销。论文指出，在共享 DDR 架构下，数据在 CPU cache 与 DRAM 之间的分布状态直接影响 FPGA 访问路径与带宽表现；而不同 I/O 接口（HP、HPC、ACP）所采用的 coherency 机制，会在 cache snoop、cache line 分配、flush/invalidate 以及 memory barrier 等环节引入额外成本，从而导致性能在不同应用场景下产生显著差异。

为系统刻画这一问题，作者将 coherency 开销拆分为“硬件成本”与“软件成本”两个维度：硬件成本体现在 cache snooping 与 cache line 分配带来的原始带宽下降，软件成本则来源于手动 cache maintenance 指令与内存屏障对 CPU 执行路径的干扰。在 Zynq UltraScale+ 上，HP、HPC 与 ACP 均为 128-bit 宽、300 MHz 运行，对应理论峰值带宽约 4.8 GB/s。然而实测结果显示，各通道在不同方向与数据规模下存在显著差异：在 CPU→PL（TX）方向，HP 接口在 4 KB–32 MB 数据范围内基本稳定接近峰值带宽；HPC 在数据已缓存时带宽明显下降，仅在数据规模增大（>16 MB）后逐渐逼近 HP 水平；ACP 在小数据（<64 KB）场景下可接近 4.8 GB/s，但当数据规模接近或超过 1 MB L2 cache 容量后，由于频繁 cache eviction，其带宽迅速下降至明显低于 HP 的水平。在 PL→CPU（RX）方向，HP 与 HPC 均可稳定达到接近峰值带宽，差距约在 5% 以内，而 ACP 同样呈现出对 cache 命中率高度敏感的特性。

在软件成本方面，论文进一步指出，非 cacheable 内存虽可避免 coherency 维护，但 CPU 对其读取带宽相比 cacheable 内存最高可下降至约 1/30；而采用 HP(C) 模式时，每次 DMA 前后的 flush/invalidate 以及 memory barrier 操作在小数据场景下可占据数据传输时间的主要比例，成为显著瓶颈。通过对 CPU→PL 与 PL→CPU 两个方向分别进行原始带宽测试与数据访问模式分析，论文揭示了不同接口在数据规模与 cache 命中状态变化下的性能转折点：ACP 适用于小规模、短生命周期数据；HPC 更适合中大规模数据传输并可避免软件一致性维护；HP(NC) 在顺序写入场景下可获得最高原始带宽但牺牲 CPU 端访问效率。

基于上述定量实验结果，作者提出了一种决策树式接口选择策略，根据数据规模（如 <64 KB、>16 MB）、访问方向（CPU→PL 或 PL→CPU）以及访问模式（顺序或随机）动态选择最优 I/O 接口与内存属性，在多个案例（如 DoG、SGEMM、CHaiDNN）中实现平均约 20% 的端到端性能提升。
![[Pasted image 20260225155732.png]]
![[Pasted image 20260225160905.png]]
# Efficient Fine-grained Processor-logic Interactions on the Cache-coherent Zynq Platform(2019)
系统评估了在 cache-coherent Zynq SoC 平台上实现细粒度 CPU–FPGA 协同的可行性。不同于传统的“批量数据 offload”模型，该工作关注于频繁、低带宽、短时运行的操作是否仍能从硬件加速中获益。作者以 seL4 微内核的任务调度器为案例，将优先级队列管理逻辑迁移至 FPGA，并分别比较了三种通信方式：基于 GP 端口的 memory-mapped IO、基于 ACP 的 cache-coherent 共享内存，以及二者的混合策略。

实验结果表明，单一通信方式难以优于高度优化的软件实现，但通过精心设计的混合通信结构（CPU→FPGA 通过 GP，FPGA→CPU 通过 cache-coherent 共享内存），可实现最高 5.5% 的执行时间提升，并将执行时间抖动降低达 58%。研究进一步指出，在细粒度交互场景中，通信模式的选择往往比加速逻辑本身更关键；同时，即使使用 cache-coherent 机制，同步开销（如内存屏障与信号机制）仍可能抵消部分性能收益。该工作证明，紧耦合 CPU–FPGA SoC 不仅适用于粗粒度加速，也能够在适当设计下改善极短运行任务的性能与确定性。

![[Pasted image 20260226110114.png]]
# Duet: Creating Harmony between Processors and Embedded FPGAs(2023)
其内部具备 load–compute–store 的数据流流水线，但在主机侧（CPU）并未提供真正的异步命令队列、多任务 in-flight 管理或事件驱动的完成通知机制。任务通常以“启动—等待—返回”的方式执行，缺乏系统级的 CPU–FPGA overlap 调度能力。此外，VTA 的运行时主要关注编译期 tile 决策与算子映射，对于共享缓冲区生命周期管理、cache flush/invalidate 优化、通信开销分解与细粒度高频调用场景下的同步成本控制，尚未形成系统化的运行时策略。

Duet（HPCA 2023）则从硬件体系结构层面指出，在细粒度加速与频繁 CPU–FPGA 交互场景下，通信与一致性维护路径可能成为关键瓶颈。该工作通过 Proxy Cache 与 Shadow Register 等机制，将慢时钟域与一致性处理从关键路径中隔离，显著降低通信延迟并提升带宽。Duet 的实验结果证明：当同步与通信开销被有效后台化后，系统整体性能可以获得显著提升。然而，Duet 主要解决的是硬件层的 cache-coherent 集成问题，并未涉及运行时层面的共享内存管理与异步调度机制。因此，在不修改硬件结构的前提下，从运行时角度构建支持异步命令队列、双缓冲流水以及一致性优化的 CPU–FPGA 共享内存 runtime，仍具有重要研究空间与实践意义。

# A Study of Pointer-Chasing Performance on Shared-Memory Processor-FPGA Systems(2016重要zynq7000)
这篇论文系统研究了在共享内存 CPU–FPGA 架构下，指针追踪型工作负载（pointer-chasing）的性能特征。作者通过参数化的 linked-list 遍历模型，分析了节点布局、payload 大小、数据依赖以及并发遍历对性能的影响。实验表明，在单链、小 payload、随机布局场景下，性能完全由 memory latency 决定，FPGA 软逻辑内存接口在此场景下表现最弱；通过 interleaving 多个遍历可以有效隐藏 latency，而引入 tightly-coupled processor 进行 hybrid 协同遍历则是提升性能的有效路径。该工作为共享内存 CPU–FPGA 系统在 irregular parallel workloads 下的运行时设计提供了重要的性能建模基础。

# NEURAghe: Exploiting CPU-FPGA Synergies for Efficient and Flexible CNN Inference Acceleration on Zynq SoCs（2018）
NEURAghe 更像是一套面向 Zynq SoC 的端到端 CNN 推理“系统原型”，其卷积引擎（CE）与软件栈（NeuDNN）为自研实现，并非行业标准 IP。然而该工作的重要价值不在于 CE 的可复用性，而在于提出并验证了 CPU–FPGA 协同的执行模型：通过异步命令队列与设备端调度，将细粒度 DMA/计算控制从 CPU 侧迁移到加速器侧，并利用跨帧流水实现对 memory-bound 阶段的隐藏。这为后续研究共享内存 runtime 的队列抽象、双缓冲重叠、以及边界开销建模提供了可借鉴的系统结构与设计思路
![[Pasted image 20260226191102.png]]
# Toward Full-Stack Acceleration of Deep Convolutional Neural Networks on FPGAs（2022）
本文提出了一种面向主流 CNN 拓扑结构的统一流式 FPGA 加速架构，通过将卷积、反卷积与全连接层映射至单一硬件模块，并结合高效的通道并行策略与智能缓存设计，实现了对残差与拼接结构的高效支持。在 Arria 10 FPGA 上实现了超过 1.3 TOP/s 的吞吐率与 97% 的 MAC 利用率。该工作显著提升了 FPGA 对复杂 CNN 结构的适应能力，但其优化重点主要集中于片上计算资源利用率，而未深入探讨 CPU–FPGA 共享内存与运行时调度机制。

# A CPU+FPGA OpenCL Heterogeneous Computing Platform for Multi-Kernel Pipeline
这篇论文通过引入 kernel_chain，将原本需要多次 enqueue 的 multi-kernel pipeline 转化为一次性批量提交（batch submission），并由 FPGA 内部硬件调度模块完成后续执行，从而减少 host 侧 command queue 的频繁参与。
# Enabling Pipeline Parallelism in Heterogeneous Managed Runtime Environments via Batch Processing（2022）
copy → execute → copy-back
copy → execute → copy-back
copy → execute → copy-back
变成
Batch1: copy → execute → copy-back
Batch2:     copy → execute → copy-back
Batch3:         copy → execute → copy-back
通过 batch：
- 让数据传输和 kernel 执行并行
- 吞吐量由“串行 latency”变成“steady-state throughput”
Blanaru et al. 通过引入 batch processing 和并行 command queue 机制，使数据传输与执行阶段形成 pipeline，并通过批量提交任务摊薄调度与边界开销，从而提升整体吞吐性能；这一“batch submit 提高 pipeline 并行度、减少调度成本”的思想，可直接迁移到 CPU–FPGA 共享内存 runtime 的设计中。

# A Pipelining-Based Heterogeneous Scheduling and Energy-Throughput Optimization Scheme for CNNs Leveraging Apache TVM
- **TVM 的异构**：更像 _“CPU 主控 + 把某些算子丢给 GPU/加速器算”_（仍然是一个整体的顺序图执行）
    
- **该论文的异构**：_“把模型切成 stage0/stage1，各自独立成‘可调用的模块’，然后对输入流做 pipeline 并发”_
这篇论文提出了一种基于 TVM 的异构流水线调度框架，通过将 CNN 拆分为多个 stage 并分配至不同处理器执行，利用 pipeline 并行与低复杂度搜索算法优化吞吐与能效。其核心思想是通过 profiling 建模 stage 执行时间，并最小化各 stage 中的最大执行时间以提高稳态吞吐。实验表明在 Jetson TX2 上可获得显著吞吐提升。然而，该工作将跨设备数据传输视为线性黑盒代价，并未深入研究共享内存一致性、DMA 开销与 buffer 生命周期管理等系统层问题。因此，该方法为异构调度提供了良好的基线，但在共享内存 runtime 与边界成本优化方面仍存在重要研究空间。
![[Pasted image 20260226232611.png]]
cpu fpga具体跑通的数值，说明跑在哪合适
cutile，triton后端实现适配？了解思路原理。
了解清楚VTA ISA



**单图时延线**

目标是：让一次推理的 total.service 变小。  
这条线不要再把 stage balance 当主目标，而是盯固定开销。

优先级建议：

1. **Replay baseline**  
    用你现在已经做出来的最小 replay 机制，先证明：
    
    - template_replay_hits 稳定命中
    - 单次 VTA 子图 wall time 下降
    - device_run_wait_us 基本不变  
        这说明降的是 runtime 开销，不是设备执行时间。
2. **把 replay 从 unittest 推到真实单 VTA 子图**  
    不要先上整图，先挑一个 ResNet18 里固定 shape、重复执行的 VTA 子图。  
    目标是：
    
    - 对真实子图做 capture/replay
    - 看单次子图 latency 能否下降
3. **再评估整图里 replay 的可见收益**  
    如果单个 VTA 子图确实降了，再看它对整图 total.service 能贡献多少。  
    如果整图还是主要卡在 stage0_cpu.run，那 replay 会是小优化，但仍然有意义。
    
4. **补充时间分解**  
    对单图 latency，重点记录：
    
    - host wall time
    - device_run_wait_us
    - template_replay_us
    - driver_submit_mmio_us
    - driver_poll_wait_us（板端）

这条线最后想回答的问题是：

**单图推理里，VTA 子图的固定 runtime 开销到底能压掉多少。**

---

**多帧吞吐线**

目标是：让 steady-state throughput 更高。  
这条线 replay 不是主角，pipeline 才是主角。

优先级建议：

1. **先固定当前最好切图**  
    你刚才说大概率是 three_stage_e，先把它当吞吐 baseline。  
    不要一边改 replay 一边改切图，不然结论会混。
    
2. **重新确认 stage run balance**  
    用真正的吞吐口径看：
    
    - stage0_cpu.run
    - stage1_vta.run
    - stage2_cpu.run  
        以及你当前使用的 CPU 线程分配是不是合理。
3. **先解决“CPU 忙等 VTA”**  
    如果现在还是一个线程在 VTASynchronize 阻塞，那多帧流水还不是真正流水。  
    这条线下一步重点应该是：
    
    - 单 VTA executor
    - 多 CPU worker
    - 多帧任务排队
    - CPU 不直接忙等 VTA
4. **再谈 slot**  
    一旦 executor 模型明确，再做：
    
    - 固定 slot
    - 每帧一个 slot
    - CPU/VTA 通过 slot 交接

这条线最后想回答的问题是：

**在 4CPU + 1VTA 下，怎么把三段真正流水起来，而不是串行分段执行。**

### 2026-09-06 P8C 实现结果

当前已在固定 ResNet-18 三 stage 候选上实现两条异构边的 slot runtime。每条边预分配两个
u-dma-buf slot，队列只传递 edge/slot/generation/frame owner，状态按
`FREE -> PRODUCER_WRITING -> READY -> CONSUMER_USING -> FREE` 转移。两种交替输入的逐帧输出、
地址对齐/不重叠、双 slot 轮换和最终释放均通过检查。

后续统计审计发现，旧分析把相邻完成间隔的中位数误当成稳态 II。流水线完成时间呈突发分布时，
这个统计会忽略 block 内的长空洞。修正口径为每个 block 使用
`(末次完成时间-首次完成时间)/(N-1)`，再对平衡 block 取中位数。三个独立 boot 的普通 copy II
为 `90.712/92.013/90.453 ms`，双 slot zero-copy II 为 `91.392/91.743/91.714 ms`；相对 FPS
差值为 `-0.74%/+0.29%/-1.37%`，均值 `-0.61%`。B0-B2 的 II 差值均值为 `-0.557 ms`，配对
Student-t 95% 区间为 `[-2.477, 1.363] ms`，包含 0。因此旧 `+3.90%` 结论已经废止，当前不能
声明端到端吞吐提升。

组件收益应与端到端收益分开表述。两条异构边界的普通 framework 物化/API 服务时间平均为
`1.446899 ms/frame`，共享 slot 绑定/发布平均为 `0.041830 ms/frame`，减少 `1.405069 ms`
（`97.11%`），其95%区间为 `[1.368642, 1.441496] ms`。每帧 framework materialization bytes
由 `1806336` 降至 `0`，减少100%。这里消除的是 Executor 间的冗余中间副本，不是全部DDR通信；
VTA内部 `LOAD 12025856 + STORE 1229312 bytes/frame` 保持不变，即共享DDR与片上SRAM之间的必要
搬运仍然存在。

### 2026-09-06 Top-20 外部有效性扫描

随后在同一 boot 上对冻结 Top-20 全部执行 B0 普通拷贝与 B2 双 slot zero-copy 配对实验。实验
发现部分切点跨越残差 tuple，一条 stage 边界包含两个张量；因此 slot manager 已从“每条边一个
buffer”扩展为“每个逻辑 slot 原子拥有该边的全部 tensor buffer”。两个张量共享同一
frame/generation 状态，必须一起发布、消费和释放。20/20 候选均通过输出等价、双 slot 使用、
物理地址对齐和不重叠检查。

这一轮把“内存工作量减少”和“最终 FPS 提升”区分开了：

- B2 每帧消除 `1.81--2.61 MB` Executor 间 framework materialization，边界 API 服务时间平均
  减少 `96.59%`，这是直接、稳定的 zero-copy 组件收益。
- 按完整 block 窗口重分析，端到端 FPS 增量均值为 `-1.20%`、中位数为 `-1.09%`，范围
  `-8.51%--+3.27%`，仅 `6/20` 为正；`14/20` 的两个配对 block 同方向，`12/20` 的 B0/B2
  block spread 均不超过 5%。所以这一轮不能证明
  zero-copy 对所有切图都提高吞吐。
- 固定 rank-1 本轮为 `-1.03%`。copy 被其他更慢工位覆盖时，节省的组件时间不会等量反映到
  Pipeline II；逐帧间隔中位数只保留为调度抖动诊断，不能再用于 FPS。

完整结果见 `vta/tutorials/frontend/report_out/resource_aware_maxplus/v1_p8_top20_zero_copy/`。
若需要形成“跨切图普遍提升”的论文结论，必须先控制当前 block 级波动，再按四种唯一 topology
选代表完成多 boot，而不能把同一 boot 的 20 个候选当成独立重复。

### 2026-09-06 VTA 锁等待计时审计

针对旧扫描中 rank 20 的后一个 VTA stage 看似从约 `20 ms` 增长到 `45--53 ms` 的现象，进一步
审计发现这不是 VTA 真正执行变慢。B0 在 stage 计时前等待全局 VTA mutex，B2 却把锁等待包含在
stage 计时中，二者原本不可直接比较。runner 已将以下量拆开：

- `vta_mutex_wait_ms`：等待唯一 VTA 使用权的时间；
- `vta_mutex_held_ms`：占有全局 VTA 锁的时间；
- `vta_run_call_ms`：真正调用 GraphExecutor `run` 的时间；
- `scheduled_service_ms`：包含锁等待的完整 stage 调度服务时间。

使用修正的 VTA 计时口径复测 rank 20 时，B0/B2 的两个 VTA `run` 分别约为
`49.63/49.69 ms` 和 `20.11/20.15 ms`，实际设备工作没有增加。B2 每帧消除 `2.609 MB` 中间
物化，边界 API 时间由 `2.176 ms` 降到 `0.115 ms`。按完整 block 窗口重分析，第一个 boot 的
B0/B2 II 为 `95.023/95.150 ms`，相对 FPS `-0.13%`；第二个 boot 为
`94.523/94.973 ms`，相对 FPS `-0.47%`。旧 `+2.31%` 来自逐帧完成间隔中位数，已经废止。

两个 boot 的完整窗口结果都接近 0，说明约 2 ms 的边界服务节省没有落在稳态关键路径上。当前
排除的是“zero-copy 让 VTA 本身变慢”，尚未证明的是端到端 FPS 提升。正式论文只应声明边界
API 服务减少、物化字节归零和运行时正确性，并把 rank 20 作为计时归因证据。

为排除短 block 偏差，随后在同一健康 boot 对 rank 1 和 rank 20 各执行两个 100 帧完整窗口。
rank 1 的 B0/B2 II 为 `91.684/92.024 ms`，相对 FPS `-0.37%`；rank 20 为
`96.734/96.548 ms`，相对 FPS `+0.19%`。两个候选的两次配对方向均不一致，而边界 API 服务仍
减少 `95.96%/93.24%`，VTA 真正 `run` 时间基本不变。这进一步支持：zero-copy 没有稳定负作用，
但当前节省的边界 copy 不在流水线吞吐关键路径，不能据此声称 FPS 提升。

### 2026-09-06 三 VTA-Island 单帧时延实验

稳态 FPS 不是 zero-copy 的唯一合理指标。若研究问题是“一张图片经过多次 CPU-VTA 交接需要多久”，
必须让普通路径与 zero-copy 路径使用相同的串行 schedule，不能把普通流水线和 zero-copy 串行模式
混在一起比较。因此本实验定义为：

```text
L0：7 个 stage 逐段串行，边界使用普通 get_output/set_input 复制
L1：同一 7 个 stage 逐段串行，边界使用单 slot zero-copy
```

冻结 stress case 为
`CPU(0)-VTA(1..3)-CPU(4)-VTA(5..8)-CPU(9)-VTA(10..17)-CPU(18..20)`，共有 3 个
VTA island、6 条 CPU-VTA 边界。部分残差切点一次传两个张量，slot manager 会把这些张量作为一个
逻辑 slot 原子发布和释放。L0/L1 的模型、输入、stage、CPU threads、VTA schedule 和执行顺序
完全相同，唯一变量是边界是否物化。

第一个 boot 采用 `L0/L1/L1/L0` 平衡 block 顺序，每 block 10 次预热、30 帧计分。所有输出等价、
单 slot generation、物理地址对齐/不重叠和前后板卡检查均通过。先以最快正确 `memcpy` 作为普通
复制基线，结果如下：

| 指标 | L0 普通复制 | L1 单 slot zero-copy | 差值 |
|---|---:|---:|---:|
| 单帧中位时延 | 167.399 ms | 162.662 ms | 减少 4.737 ms（2.83%） |
| 边界 API 服务中位数 | 4.484 ms | 0.111 ms | 减少 4.373 ms（97.52%） |
| framework materialization | 9,031,680 B/frame | 0 B/frame | 减少 100% |

两组 L0 block 中位数为 `167.703/167.205 ms`，两组 L1 为 `163.057/160.959 ms`，两个配对方向
均为正。能直接归属给 zero-copy 的保守组件收益是 `4.373 ms` 边界 API 服务和 `9.032 MB` 物化
字节；端到端多出的约 `0.365 ms` 不强行解释，保留为运行波动。

为与历史 200 条数据使用的 runner 默认行为对齐，又在同一 boot 撤销
`AXU5EVB_DRIVER_SAFE_COPY=0` 覆盖，保持候选、输入、顺序、warmup 和计分次数不变。结果如下：

| 指标 | L0 runner 默认安全复制 | L1 单 slot zero-copy | 差值 |
|---|---:|---:|---:|
| 单帧中位时延 | 178.855 ms | 163.093 ms | 减少 15.762 ms（8.81%） |
| 边界 API 服务中位数 | 16.324 ms | 0.111 ms | 减少 16.213 ms（99.32%） |
| framework materialization | 9,031,680 B/frame | 0 B/frame | 减少 100% |

该切法在旧 profile 中的 direct copy 为 `17.239 ms`，与新测 `16.324 ms` 接近。此前三-island
历史分组的 `11.366 ms` 是 109 个不同切法的中位数，并非任意三-island方案的固定开销。默认安全
复制结果用于和历史数据对齐；评价 zero-copy 相对优化后普通实现的独立收益，仍采用更严格的
`memcpy` 基线。两种结果目前都只有一个 boot。

这个三 island 布局是 communication-intensive stress case，用于放大并验证多次边界交接的累计
代价，不是生产最优切图，也不能用来主张“应该增加 island”。它同样不消除 VTA 在共享 DDR 与片上
SRAM 之间的必要 LOAD/STORE。当前结果只来自一个 boot；至少完成三个独立 boot、配对 95% 区间
排除 0 后，才可正式声明稳定的单帧时延提升。产物位于
`vta/tutorials/frontend/report_out/resource_aware_maxplus/v1_p8_latency_island_scaling/`。

### Zero-copy 的适用规律：降低时延不等于提高流水线吞吐

当前结果应准确表述为：**串行单帧时延有下降，但自然 Top-20 的稳态流水线吞吐没有显著改善。**

串行推理中，CPU stage、边界复制和 VTA stage 按顺序执行，六条边界都在这张图片的关键路径上：

```math
L_{serial}=\sum_i T_{stage,i}+\sum_e C_{boundary,e}+T_{runtime}.
```

所以删除一条边界的复制，通常会直接减少这张图片的完成时间。三-island实验中，优化 `memcpy`
基线的边界 API 服务减少 `4.373 ms`，端到端中位时延观察到减少 `4.737 ms`；两者接近，符合这种
累加关系。

流水线同时处理多张图片时，吞吐周期不是所有 stage 时间之和，而主要由最慢工位或最忙共享资源
决定：

```math
II=\max(T_{stage/resource}).
```

如果边界复制所在工位仍比瓶颈工位快，复制就存在可被隐藏的 `slack`。此时 zero-copy 虽然减少了
CPU/DDR 工作和物化字节，却没有缩短最慢工位，因而 FPS 基本不变。可近似写为：

```math
\Delta II\approx\max(0,\Delta C_{boundary}-slack_{boundary}).
```

P8C 的三个独立 boot 正好呈现了该现象：边界 API 服务平均减少 `97.11%`、framework
materialization 减少 100%，但 `B0-B2` 的 II 差值 95% 区间为
`[-2.477, 1.363] ms`，包含 0，因此不能声称吞吐提升。这不是 zero-copy 失效，而是节省发生在
非瓶颈路径。只有切图产生更多或更大的边界、boundary/DDR 成为关键资源，或者系统目标是单张
图片时延时，zero-copy 的时间收益才会直接显现。

论文据此报告三层证据：三 boot 的边界组件收益是正式结论；三-island串行 `2.83%` 是单 boot
stress-case观察；自然 Top-20 没有显著 FPS 提升是受控负结果。除非需要把 `2.83%` 升格为跨 boot
结论，否则不再追加相同实验。
