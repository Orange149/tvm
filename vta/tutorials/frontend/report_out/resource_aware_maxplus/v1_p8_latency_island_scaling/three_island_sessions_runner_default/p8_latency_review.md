# P8 串行单帧时延审查

更新日期：2026-09-06

## 实验定义

L0 与 L1 使用完全相同的逐 stage 串行调度。L0 使用普通 `get_output/set_input` 物化，L1 使用单 slot zero-copy；本实验不使用双 slot，也不把流水线重叠算作收益。

L0 普通复制策略：`runner_default`。

本地审计：`通过`。

## 最新 Boot

- boot：`1617c571-1b81-41d9-9eff-b7224dd9d5c4`；VTA island：`3`；功能 gate：`通过`。
- L0/L1 单帧中位时延：`178.855/163.093 ms`。
- 单帧时延差：`15.762 ms`（`8.81%`）。
- 边界 API 服务：`16.324 -> 0.111 ms`；每帧消除物化字节：`9031680`。

该结果来自一个 boot，只是 qualification；至少三个独立 boot 且配对置信区间排除 0 后，才能正式声称单帧时延提升。

独立 boot 数：`1`；正式时延声明：`不允许`。
