# C1 收尾与 C2 共享缓冲增强执行清单

2026-09-08。唯一任务定义和验收门槛见 [主计划](ONE_MONTH_EXECUTION_PLAN.md)，
旧支路见 [历史归档](HISTORICAL_EXECUTION_PLAN_20260908.md)。

## C1：只收尾，不扩大研究

- [x] 摘要及贡献段明确 FuseOps/量化/packing/AutoTVM 与本文外层搜索的职责。
- [x] 固化已有结果及其证据范围，见 [收尾证据](C1_CLOSEOUT_EVIDENCE.md)。
- [x] 核对 M1/M2 的边界服务来自 set/get copy 路径；不冒充重新标定的 shared-K2 成本。
- [x] 保留历史计算二进制，未用 E3 新 tail 替换旧性能基线。
- [ ] 最终展示配置的统一精度政策/反例逐帧回归仍有缺口；在关闭前限制跨切图共同数值语义主张。
- [ ] 论文全文最终交叉引用及文字复核（局部已修正，尚非全文定稿）。

延期而非完成：无界 AutoTVM、去屏障 VTA、全域 E6-R、G1–G3、完整 C3-B。
2026-09-10 只恢复有界的编译期传输签名引导安全调优，TopHub 始终作为 incumbent。
这些不再是当前 C2 缓冲增强的前置条件。旧产物保留 C3-S 命名以维持引用，
不再独立拆分第三贡献；原 DDR 争用研究保留为探索结果，不恢复门控控制器。

## S0：已通过进入受限实现的门槛

- [x] A/B/C/D rank01/05/07/13 原图、参数、计算库、运行库逐文件哈希核对。
- [x] 可执行静态审计；10个拒绝/安全规则单元测试通过。
- [x] 找到5个独占 CPU→VTA 输入池候选；内部别名、参数、未知和反向边保守回退。
- [x] 跨帧源码审查：slot1 绑定后不能有其他 entry 继续读写原 slot0；owner 和物理检查仍须 S1 实现。
- [x] 隔离构建/部署诊断 driver、runner，原基线未覆盖；只读快照不初始化池。
- [x] 四拓扑、三模式共12次独立进程烟测，264个正式帧输出哈希与旧路径相符。
- [x] 四拓扑 graph pool 与额外 K2 分配全部逐字节对账；warmup 后至22帧结束增长为0。
- [x] 发现并计入额外64 MiB运行时队列；不把 slot 节省比例冒充总池内存节省比例。

历史可行性结果：[S0 报告](c3s_buffer_reuse/EXPERIMENT_S0.md)。
当时的预测现已由 [S1 实测](c3s_buffer_reuse/EXPERIMENT_S1.md) 对账：
slot 可省36.36%–44.44%，总高水位仅约1.03%–1.21%，不等于提高FPS或释放192MiB预留。

## S1：受限实现及32帧资格化已完成

- [x] 可选 `--shared-buffer-plan`，核对实际 stage graph/lib/params 和已加载 runtime/driver 的 SHA-256。
- [x] 安全边 pool-anchor slot0 + external slot1，其余 external-K2。
- [x] 多张量整边回退、物理范围/对齐/跨边冲突启动验证；完整负例覆盖仍见 S2。
- [x] owner 持有和销毁顺序；不改变四态、generation、bundle 或 K2。
- [x] 四拓扑同一新 runner 开关对照，实际新增 slot 与总高水位节省均与预测逐字节一致。
- [x] 每拓扑8个不同输入循环32帧输出逐字节一致；A 的错误entry整边回退，错误graph哈希在加载前拒绝。
- [x] 23个原生规则检查 + 10个静态审计测试通过。

## S2：进行中，不得提前宣称完整通过

- [x] 规则级物理拒绝/别名/参数检查，板端错误哈希拒绝与entry错误整边回退；自然 slot1/slot0 交错已覆盖。
- [x] A/B/C/D 每拓扑8输入循环1000帧，合计4000帧/16000000B输出与原路径逐字节一致，高水位无增长。
- [x] 异常退出边界已测：直接重启失败，重载同一 bitstream 后 A/B/C/D 恢复正确。
- [ ] 强制慢消费者全拓扑和完整多张量部分不合格板端负例按 2026-09-10 范围决策停止；不得写成通过。
- [x] 四个冻结拓扑均达到实际新增 slot 节省≥20%；全部报告总高水位且未增加分配。
- [ ] 三 boot 非劣效验收按范围决策停止；boot1 完整、boot2 仅 8 个完整块，不声称 95% 性能保护。
- [x] 定位为 C2 增强，不复现 USMP，不另立独立第三点；如实报告总高水位约1%的实际收益。

原 S2 自然压力已结束；当前队列实验状态见下节，不以旧运行结束状态描述新实验。
自然压力结果见 [S2 报告](c3s_buffer_reuse/EXPERIMENT_S2_NATURAL_STRESS.md)。
后续独立 boot 由用户配合，不擅自重启。

## Q0/Q1：共享队列容量实验（新增当前支路）

- [x] 两类 backing 容量独立配置，默认各 32 MiB；未改全局 VTA_MAX_XFER 或提交策略。
- [x] 可关闭的逐提交诊断；四拓扑峰值均为指令 5312 B、微指令 2792 B。
- [x] 两倍余量且 256 B 对齐：10752 B + 5632 B = 16 KiB。
- [x] 四拓扑 32 帧三模式对照通过，每进程实省 67092480 B，总高水位下降 82.23%–86.96%。
- [x] 指令/微指令容量不足、未对齐、未资格化提前提交阈值的板端拒绝通过。
- [x] 每拓扑 1000 帧缩容自然压力，合计 4000 帧/16000000 B 输出逐字节一致，高水位无增长。
- [x] Q2 依赖/微指令跨批资格化与四阈值扫描完成，冻结 T2656/11008 B。
- [ ] E↔P、P↔F 三 boot 正式性能保护停止；仅保留 boot1 和 boot2 完整块的描述性数据。

原始结果、失败记录及统计修正见 [队列报告](queue_capacity/EXPERIMENT_QUEUE.md)。
该空间优化并入 C2，不另立第三贡献；没有改模型参数/图内存、tile 或硬件。
本轮 Q1 全部测试进程已退出，未重启；16 KiB 尚非最终 F，不能预先称为最优或性能无损。

## 2026-09-09 续跑

- [x] 用户重启后恢复原模块和 bitstream；SD 已满，不删除历史产物，诊断/性能新日志改放 tmpfs。
- [x] 两轮边界审计各 256 帧数值对照通过；第二轮 2006 个完整收尾副本与实际指令哈希一致。
- [x] 修正收尾前门槛过严的解释：加入原 Synchronize 收尾后存在闭合候选，不能说没有提前提交机会。
- [x] FIFO 可调度性与 SRAM/uop 跨批资格化、真正阈值扫描完成。
- [x] Q1 固定 16 KiB 的首个独立性能 boot 已完成；不继续三 boot 推断。

此节替代上节“当前已退出”的运行状态描述。结果与限制见
[Q2审计](queue_capacity/EXPERIMENT_Q2_AUDIT.md)，不要在性能任务仍运行时重启开发板。
