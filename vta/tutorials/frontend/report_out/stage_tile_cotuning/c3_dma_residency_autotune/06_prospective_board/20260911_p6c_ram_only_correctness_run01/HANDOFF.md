# Handoff

P6c is complete and immutable. Use `results.jsonl` as the per-candidate/per-seed evidence and `summary.json` as the acceptance record. The accepted input hashes are recorded in P6d's preregistration.

The next allowed measurement is the P6b-frozen 30-round balanced crossover timing run. Any future mechanism claim must retain the distinction between correctness, reduced requests, and reduced latency.
