# Handoff

P6e resolves P6d's configuration confound: five of six fixed-ConfigEntity mechanism pairs exceed the frozen 5% improvement threshold, and the sixth improves 4.33% but remains formally indeterminate. All 33 correctness checks and 360 timing samples completed in the same qualified RAM-only boot.

The next step is no longer another hand-picked mechanism example. Build the P7 search evaluation around the observed two-level effect:

1. residency mode can improve a fixed schedule;
2. a DMA-minimal schedule can still be slower than the TopHub incumbent.

The selector therefore needs both traffic/request/synchronization features and compute/tile/thread features, with incumbent protection. Evaluate it on the frozen grouped holdouts and equal board-dispatch budgets before claiming a standalone autotune innovation. Repeat only final selected comparisons across at least three boots before making stable performance claims.
