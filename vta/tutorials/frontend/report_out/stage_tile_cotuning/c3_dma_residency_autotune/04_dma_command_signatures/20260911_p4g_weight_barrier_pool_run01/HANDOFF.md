# HANDOFF

Use `results.jsonl` for static DMA, explicit sync, dependency, and FSim evidence. Stable IDs bind the mode, full ConfigEntity, workload, hardware fingerprint, and schedule/compiler source hashes. Static DMA reduction and FSim correctness do not establish board speed. Do not merge this mode into production candidates without a separate admission decision.
