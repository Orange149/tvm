# HANDOFF

Use `results.jsonl` only as candidate-level local FSim command-shape evidence. The signature hash excludes addresses and all timing fields. A separate board run must establish board-runtime equivalence, safe queue capacity, and any performance claim. Negative or absent diagnostics are frozen rather than imputed.
