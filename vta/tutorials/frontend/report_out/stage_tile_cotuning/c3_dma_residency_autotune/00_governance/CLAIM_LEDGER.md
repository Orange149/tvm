# C3 主张账本

本文件区分“已知事实”“待验证假设”和“禁止主张”。任何负结果都通过降低主张等级处理，不回改门槛。

| ID | 当前状态 | 可主张内容 | 成立所需证据 | 失败后的口径 |
|---|---|---|---|---|
| F0 | 已有证据 | TopHub 是已有硬件记录的强质量参照，不是本文必须击败的对象，也不作为在线搜索器输入 | 10/10 workload 命中、历史 topology-B 证据；使用前仍须核对硬件指纹与正确性 | 若已有精确可用记录则直接部署；研究搜索效率时仅作隐藏的事后参考线 |
| F1 | 已有证据 | Lowered TIR 可精确给出当前模板的逻辑 DMA 请求字段 | 8/8 direct-template 六字段与 runtime 一致 | 仅限已核对模板和字段，不外推 AXI |
| F2 | 已有证据 | 当前 TopHub tile 存在输入/权重重复加载 | 10 类 workload 的 reload 统计 | 只说明存在空间，不等于新 schedule 会更快 |
| F3 | 已有板端证据 | 当前 boot 已完成 ext4 修复与写入资格，活跃 VTA 实验仍可使用 RAM-only 路径减少 SD 磨损 | SD recovery；post-repair G2；P7Q 全程 tmpfs；4057 秒水位后无新增存储错误 | 冷启动资格仍未完成，仅当前 boot 有效 |
| R0 | 全文已取得，部分机制为功能级独立重实现 | Cheng 2026 的 Algorithm 1/2、Fig. 1--3、Table 1/2 已核对；input-prioritized、四方案、资源条件与 TIR/runtime weight-reuse 路径均不属于本文原创。本地 input 模式可做结构映射，但 `weight_resident_barrier` 与论文的不覆盖地址/条件 LOAD WGT/PushGEMMOp 实现不同 | 若主张完整机制复现，需实现论文式地址/UOP语义并与 barrier 方案消融；作者源码和 commit 不可得，不能称 code-exact | 可主张相同高层机制的独立实现和本平台结果；禁止把 barrier 结果冒充论文实现，或把论文四方案、SRAM fallback 写成本文原创 |
| C3-H1 | 单算子配对进一步支持 | 调整 loop order/cache lifetime 可减少目标 LOAD/reload；固定 ConfigEntity 时可产生局部板端加速 | P6e 六组；P7Q 次分析 56 组中 42 组更快、中位 3.31%、22 组不少于 5% | P7Q 次分析为单 boot、post-hoc secondary；不能替代跨 boot 确认 |
| C3-H2 | P7Q 不支持最终赢家主张 | 当前冻结池的四个 pool oracle 全为 original TopHub | 至少两类 workload 的稳定最优模式不同，模式差异至少 2% | 只能报告同 tile 模式收益随几何变化，不能声称按层最终模式选择已成立 |
| C3-H3 | P7Q 未通过 | B6 full request signature 未优于 B5 bytes-only；budget 16 反而仍有 1.33% 平均 regret | 新的未见确认集上 full-signature 优于 bytes-only | 保留请求形态的诊断价值，不声称其已提高搜索效率 |
| C3-H4 | 仅 incumbent 保护成立 | B7/B8 以首派发保留 4/4 pool oracle；但 B4 同样因枚举顺序把 incumbent 放首位，无法把收益归因于联合硬件特征 | 新协议补齐 residency×virtual-thread 候选并使用未见确认集 | 当前仅主张安全防退化策略；不主张通用联合调优已成立 |
| C3-H5 | 本轮无新增可证书化胜者 | P7Q 无 non-incumbent ±2% 候选，因而未启动新的 backing 缩容压力测试 | 新胜者的 runtime peak、FINISH/replay、32 帧拒绝和 1000 次压力 | 沿用 C2/旧 schedule 证据，不把它包装为 C3 新结果 |
| C3-H6 | G7 后停止 | 无新 stage overlay，按冻结计划不进入 P8/P9 | stage correctness、同 boot 配对、至少 3 boot 最终确认 | 只报告单算子机制与候选池搜索边界，不声称 FPS 提升 |
| C3-H7 | 未见验证否定当前 request-shape 排序器 | W04 config455/config461 的同 bytes/calls 异号效应仍说明请求形态有诊断价值；但 E02 冻结 shape/control 相对同 tile original 分别 -14.80%/-13.18%，0/7 获胜，前瞻符号预测失败 | 重新建立包含 overlap/compute 和真实硬件标签的规则后，另用未见几何验证 regret@budget | 只主张 bytes/calls 不充分；明确报告当前 score 失败，不主张通用预测 |
| C3-H8 | 未见解析证书支持大幅板前过滤 | E00/E01/E02 共 2368 个 t2 联合配置仅 209 个通过 lowering、容量、复用和 input-DMA 证书，解析保留率 8.83% | 证明过滤后的候选能以同预算保持 near-oracle，且正确性门不漏掉最终强候选 | 当前只能主张减少“进入下一资格门”的候选，不等于减少达到最优所需搜索成本 |
| C3-H9 | 双路径命令/FSim与 FPGA canary 的必要性已证实 | 11 个代表 original 的补测中 10 个三 seed FSim 正确，E00 config1081 的重复 UOP `dst_idx` 被 host/runtime 检查提前拒绝；E00/E01 其余多个 original 虽 FSim 正确仍在 FPGA 错误，E02 15/15 正确 | 把可计算 UOP 规则集成到候选生成；更多已知安全 workload 验证；未建模几何保持 TopHub 回退 | 可主张层级合法性证书和安全回退必要；不可声称已有完整静态 FPGA-correctness 公式 |
| C3-H10 | DMA Pareto 规则回顾性有效、E03 前瞻止于正确性门 | 输入驻留只有在输入字节减少且总 DMA bytes/calls 均不增时才优先：P7Q 回看选择 11/14、11/11 更快、中位 +8.31%；E02 两个 -14.80%/-13.18% 点均 abstain。新 E03 从 864 过滤到 74，双路径 FSim 4/4 通过，但 Pareto 首选 config17 在 FPGA 0/3 正确并在计时前停止 | 找到 FPGA-correctness 可计算条件，或在新硬件指纹/几何上采用 canary 标签后评估预算-regret；另做未见、正确候选的前瞻排序验证 | 可主张 Pareto 修正避免局部 DMA 指标误导以及 canary 的必要性；不可主张 Pareto 已前瞻预测性能或 E03 获得加速 |
| C3-H11 | 一项前瞻结构迁移成功，最终 incumbent 保护生效 | W03/W05/W06 在目标标签前按已知正收益映射结构与目标 DMA-Pareto 冻结；W03/W06 的 original 虽 FSim 正确但 FPGA 0/3，均在计时前停止；W05 三段 9/9 正确，input-stationary 相对同 tile original 成对中位 +12.59%、7/7 获胜，但相对 TopHub -34.15%，最终回退 TopHub | 在 `oc_nthread=2`/TopHub 邻域找到并前瞻验证更多硬件正确候选；以固定板端预算比较 near-oracle/regret | 可主张一次新的同 tile 结构迁移和硬件证书/强基线保护闭环；不可主张候选已击败 TopHub、提升 stage/FPS 或三目标性能预测全部成功 |
| C3-H12 | TopHub 邻域小预算正证据成立 | W05 t2 纯 input-stationary 的 27 个输入降流量候选均因总 bytes 增加而 abstain；将 bounded hybrid 的虚线程从组内改为跨组映射后，400 个配置有 19 个 DMA-Pareto，按 TopHub 距离冻结 2 个。两者 12/12 FSim、4/4 交叉编译、18/18 FPGA seed；config574/494 同 tile 分别 +4.96%/+9.86%、均 7/7，冻结的 config574 最快预测成立，且距 TopHub 仅 0.31% | 在其他 workload/独立 boot 验证；或测量完整、无偏小池以报告 pool-oracle regret | 可主张 400→2 的候选派发空间压缩（99.5%）与 strongest-incumbent 2% 等价带结果；398 个未测点无 latency，不能主张完整 pool oracle、击败 TopHub、stage/FPS 或跨 boot 稳定性 |
| C3-H13 | W05 精确命令资源证书通过本地与真实 FPGA 门 | TopHub575/same-tile original574/hybrid574 的无 timing FSim 峰值分别为 3744/1480 B、7200/752 B、3808/1424 B（instruction/UOP，含 FINISH）；最终 TopHub575+hybrid574 只需 4096+4096 B。本地 3/3 通过；隔离板端 runtime 同会话查询到实际容量 4096/4096 B、峰值 3808/1480 B、6 次提交，两个身份三 seed 共 6/6 正确，默认 RPC 已恢复 | 对完整 stage 的所有提交生成同类证书；源码/形状改变时强制失效重算 | 可主张精确 W05 静态身份的真实 VTA/u-dma-buf requested backing 从 64 MiB 降至 8 KiB（99.9878%）；不能外推整网、动态形状、allocator 内部碎片或 FPS |
| C3-H14 | 集合定容规则取得跨几何本地证据 | `C_q(S)=align(max B_q)` 用同一 24 KiB instruction + 12 KiB UOP 容量覆盖 W00--W09、五模式的 197/197 冻结身份；逐身份容量中位 8 KiB、P90 12 KiB、最大 28 KiB；留一 workload 固定容量仅 9/10，W08 需额外三页 instruction | 更多模型/动态形状；完整 stage；真实 FPGA 扩展池验证 | 可主张容量应由精确编译 allowlist 与生命周期推导，而不是使用板级魔数；36 KiB 本身不是通用常数，197 池仅为本地 FSim 安全证据 |
| C3-H15 | AutoTune 语义派发与资源证书链已实现 | v2 派发使用完整 ConfigEntity/candidate/TIR 身份；W05 当前 ConfigSpace 2/2 allow-timing 身份匹配。调优三身份集合推导 12 KiB provisional，最终两身份集合推导 8 KiB qualified，证明容量随 allowlist 改变 | 其他 workload 的前瞻同类部署；动态形状需逐形状重发证书 | 可主张资源约束已在 measurement 前进入调优与部署；不可把 8/12 KiB 写成板级常数 |
| C3-H16 | 当前 runtime 的 prospective manifest attestation 已闭环 | 每个 FSim worker 绑定实际映射库哈希；pending manifest 在板端标签前冻结。隔离 ARM runtime 下两个身份 6/6 seed 正确，同会话返回同一 manifest ID、4096/4096 B 容量、3808/1480 B 峰值、6 次提交和 replay disabled；ready ID 与 pending ID 相同；capture/replay 负向控制均被 runtime 拒绝 | 完整 stage、并发队列或 replay-enabled 部署必须产生新合同和新峰值证据 | 可主张内容寻址、fail-closed 的 exact-allowlist command backing 部署方法；不主张跨 boot、整网、动态形状、物理碎片或 FPS |
| C3-H17 | 论文启发式 validity 模型出现离线开发信号 | 310 个冻结候选按 workload 留一：budget=16 时 Random/Rieber-inspired/ML²-base/ML²-hardware 平均合法数为 10.18/11.80/14.20/14.50 | 严格复现 Rieber 初始化；新 workload 预注册等预算确认；FPGA correctness 单独 canary | 可主张独立 local-validity 模型值得继续；不能称精确论文复现，也不能把 lowering-valid 当 FPGA-correct |
| C3-H18 | request/command 特征与资源联合目标在当前开发池无新增收益 | 56 个 same-tile 配对按 workload 留一时 DMA bytes 已达 regret@1=0.02 个百分点，full request 与 command/hardware 均未再改善；P7Q 79 个 latency 标签的四个 oracle 已全部使用单队列最小 8 KiB backing，各 workload Pareto front 仅一个点 | 在新、完整且存在资源差异的候选池前瞻确认 | 保留 full request 的诊断价值和 command backing 的部署价值；不主张二者已提升搜索排序 |
| C3-H19 | semantic hardware-ledger v2 本地闭环 | normalized complete ConfigEntity 进入 key，config_index 仅审计；重编号稳定，entity/TIR/hardware 变化与旧 v1 均 fail closed；相关 28 tests 通过 | 后续新板端合同用 v2 重新冻结，不静默继承 v1 pass | 可主张索引无关的证书身份实现；旧 v1 历史证据仍仅按原合同解释 |
| C3-H20 | same-tile 解析 `ΔT` 模型有开发信号但 request 结论混合 | 56 配对四折留一：mode+bytes/calls 的 MAE 0.119648 ms、Spearman 0.8356；加入 request 后 regret@1 从 0.327558 ms 降到 0，但 MAE/Spearman 变差；command 无稳定增益 | 新网络几何、预注册、完整池、多 boot 确认 | 可主张相同 tile 下建模驻留增量比绝对排序更有希望；不能主张 request/command 已普适提升搜索 |
| C3-H21 | Rieber 2022 当前只能降级复现且结果阴性 | 310 行稀疏池每 workload 仅 7 个实体/5 task，邻居闭合率 29.55%；20 seeds、16 calls 下 R1 valid 中位 10，与 Random 相同并低于静态顺序 11.5；R2 balanced E0 成功，R3 仅重排已知 legality | 完整单 task ConfigSpace、完整邻居、latency ground truth 后才可严格复现 SA | 保留为方法审计/负结果，不把 locality initialization 纳入主算法 |
| C3-H22 | USMP 式统一生命周期图因证据不足 fail closed | A/B/C/D 每项 32 帧但只见单 queue 峰值 5312/2792 B；全库 74732 条 queue records 均缺 FINISH/frame/物理地址，profiler 又是 64/200 事件有界尾部 | 按 instrumentation contract 记录 alloc/free、range、关联 ID、device_done、FINISH、replay lifetime | 不报告 naive sum 或 packing 节省；只保留 per-queue 局部峰值和所需埋点 |
| C3-H23 | 新网络确认池已在目标标签前冻结 | 三个未用于 C3 性能训练的 YOLO 真层共有 3920 个 ConfigEntity；纯硬件/结构预筛为 78/56/343。36 点 pilot 与 96 点正式池均按固定 SHA 前缀冻结，四模式严格平衡；候选承诺后才确认三个 workload 各有一条精确 TopHub 命中，未读取 entity/index/cost | 对 96 点完成 lowering/FSim/交叉编译、FPGA correctness 和完整池 timing；候选、特征、阈值不得再改 | 当前只可主张无泄漏协议和确认池已冻结；36 点 pilot 不能支撑搜索效果或 CCF-B 水平结论 |
| C3-H24 | no-leak 等 gross-budget 搜索器已实现，旧数据 smoke 对主模型不利 | selector 只能看到 public projection，失败消耗 gross dispatch；支持 Random、stock-XGB、rules、V 和 bytes/request/command 三档 `V+ΔT`，20 seeds。P7Q 开发 replay 中 rules-only 的非 reference pool regret@4 为 0%，但全部方法均未进入 TopHub +5%；V 与三档 `V+ΔT` 在小预算下未优于 rules/stock-XGB | 在 P7R115b 96 点未见确认池上做预注册同预算比较，并补齐 phase wall-clock | 不以旧 P7Q replay 声称新搜索器有效；若确认仍不优于 rules/stock-XGB，则删除复杂模型并降为规则型 VTA case study |
| C3-H25 | tile 同时决定数据访问与命令共享内存的本地证据成立 | P7R117 的 36 gross 中 20 static-pass、15 FSim-pass；正确身份的单候选 4 KiB 对齐 instruction+UOP backing 为 8,192--335,872 B、中位 36,864 B；同时 data DMA bytes/calls 随 tile/mode 大幅变化 | 新 v2 正式池的板端正确性/latency 与最终 allowlist runtime 容量核对 | 可主张容量必须由候选身份推导；不可把 8 KiB 或 335,872 B 外推为板级常数，也不可把逻辑 DMA 当 AXI 计数 |
| C3-H26 | Y02 tile--DMA 合法性边界和真实权重复用本地成立 | 硬件预筛 343 点中 original lowering 29 点、旧四模式交集 12 点；`tile_ci=1 && (tile_co=1 || tile_h in {1,H})` 在同域精确描述 original 合法性。v2 的 mode4 在 `tile_co=1/2` 将 weight 11,501,568 B、4,992/2,496 calls 降到 884,736 B、16/8 calls，并增加 16/8 个 residency drains；8/8 buildable 身份三 seed FSim 正确，`tile_co=8` 因容量失败 | 至少两个未见几何的 v2 池板端 same-tile latency；开发集冻结同步惩罚后再评价 minimum-access | 可主张 tile、padding、DMA 2-D pattern、容量与同步的可执行关系；同域 100% 不是跨 workload 泛化，mode4 不是无屏障预取或论文逐行复现 |
| C3-H27 | Y02B00 权重驻留取得强同 tile 板端正结果 | original/barrier 均三 seed FPGA 正确；7 轮平衡配对中 barrier 7/7 胜，140.989→105.349 ms（+25.28%）；总 LOAD 19,488,768→8,871,936 B（-54.48%），weight LOAD 11,501,568→884,736 B（-92.31%），同步 1→17 | 第二个未见几何的同类配对或明确相反边界；跨 boot 只作增强 | 只主张单 Y02B00 family 的同 tile 机制效果；不得写 YOLO 平均、整网 FPS、TopHub 等价或搜索收益 |
| C3-H28 | 数据访问与命令 backing 的同机制时间/空间联动成立 | Y02B00 original 的 instruction/UOP peak 为 259,760/328 B，barrier 单提交 peak 为 11,792/7,504 B；4 KiB 对齐总 backing 266,240→20,480 B（-92.31%），同时取得 H27 latency 正收益 | 对最终 selected/fallback allowlist 生成新 manifest，并做精确容量通过和不足一页 fail-closed | 可主张 barrier 同时减少数据重放并限制单提交命令峰值；不可声称释放固定 192 MiB 物理 reserve，亦不可把 20 KiB 外推为通用常数 |
| C3-H29 | TopHub 必须通过当前硬件正确性资格 | Y02 exact TopHub source 与 mode0 adapter 的 TIR/二进制完全相同，本地 FSim 均 3/3 正确，板端均 0/3；未取得候选 latency。同期 W05 config575 健康门 3/3 正确 | 找到同硬件指纹且 FPGA-correct 的 Y02 强 incumbent，或仅以完整正确池 oracle 评价 | 当前 Y02 TopHub 不能作为 latency 分母、部署 fallback 或 2%/5% 门槛；这不是“击败 TopHub”证据 |
| C3-H30 | Y01 旧 0/23 负结果已被 clean-start 恢复实验部分反驳 | 旧 P7R125/P7R126 在未清理执行状态下记录 23 个身份错误；P7R170 对预注册六点子池先停止 RPC、重载同一 bitstream、启动新 RPC 后，3/6 身份三 seed 正确并取得 latency，另 3/6 仍首 seed 错误 | 未经同样 clean-start 复测的其余 18 点不得继续计为候选 FPGA-invalid；后续板端池必须把 clean start 放在任何 tensor 分配之前 | 旧 0/23 只能作为执行状态污染历史，不再支持“Y01 几何整体不正确”；P7R170 的 3/6 结果仅适用于精确六点身份 |
| C3-H31 | Y00 本地池给出 compact/UOP 结构边界 | 24 个 v2 身份中 18/24 static-pass、11/24 三 seed FSim+命令签名通过；barrier 仅 2/8 lowering 后仍 0/8 FSim，完整三模式 family 为 0/8。六个 barrier 为 non-compact weight region；F00 original、F02/F07 三模式触发 duplicate `dst_idx` | 板端执行恢复后，按冻结结构顺序先验证一个 original/input 完整 pair；barrier 必须重新设计 compact 合法实现后发布新版本 | 可主张当前 mode4 在大空间 Y00 的编译/执行限制；不能把 0/8 写成权重驻留规律本身无效，也不能用未上板点声称 latency |
| C3-H32 | 顺序 fail-fast FPGA 认证取得共享内存/搜索成本回顾性正结果 | 29 个完整三 seed Y01/Y02 身份中，完整验证 87 次；首错即停且通过仍需三 seed时为 33 次（-62.07%），29/29 分类一致。逻辑 LOAD bytes -61.84%，LOAD calls -61.88% | 在新的冻结候选流中前瞻运行并报告首 seed 通过、后续 seed 失败的晚失败数，以及 candidate gross dispatch/FPGA invocation/wall-clock 三种预算 | 可主张在当前回放集减少重复错误执行；不得声称减少 gross candidate 数、未来零漏判或已改善 latency 搜索 regret |
| C3-H33 | 三几何 tile—共享内存因素图已统一 | Y00/Y01/Y02 共 57 个 v2 身份、38 个 same-tile 驻留对、40 个单 knob 关联对；input 模式 LOAD bytes 中位变化为 -30.51%/-5.06%/-22.55%，barrier 为 -40.16%/-83.80%/-61.50%；同时绑定 static/FSim/FPGA/timing 层级 | 扩展到更多几何；当前仅 Y02 一对有板端 timing，单 knob 对只是样本内受控关联 | 可主张 tile/几何显著改变逻辑 DMA 与容量权衡；不可把本地中位数写成 AXI、latency 或通用因果定律 |
| C3-H34 | 跨 tile 共享内存简单排序取得强开发集信号 | P7Q 4 workload、76 非 TopHub 候选、20 seeds：budget=1 的 pool-oracle+2% 成功率为 Random 20%、stock-XGB 12.5%、paper same-tile minimum-access 33.75%、bytes-only 83.75%、本文 bytes→calls→padding→submissions 100%；本文 median trials=1 | 规则已冻结；必须在新的完整 FPGA-correct 性能池前瞻确认，并补齐阶段墙钟与搜索期 DMA | 只可主张开发集算法选择依据；P7Q 标签已暴露且 75/76 可计时，不能声称未见泛化、TopHub 等价或无效候选过滤收益 |
| C3-H35 | Y00 前瞻搜索确认合同已在板端标签前冻结 | P7R132 将 24 个原始身份经 18 个 static-pass、11 个三 seed FSim-pass 冻结为板端全测池；预注册五类策略、20 seeds、budget 1/2/4/8、完整池 oracle 和 fail-fast 正确性流程；TopHub 不参与排序或 oracle | 当前启动状态恢复后，先过 W05 健康门，再完成 11/11 正确性分类、全部正确候选计时和等预算回放 | 当前只能主张前瞻协议成立；本地门结果已经暴露，前瞻性只覆盖板端正确性、性能排序和 search regret |
| C3-H36 | Y00 前瞻候选已完成无标签交叉编译封存 | P7R134 的健康探针与 11 个候选 12/12 交叉编译成功，11/11 TIR 哈希匹配 P7R132；记录 `board_contacted=false`、`performance_labels_collected=false` | 用同一合同和身份上板，不允许看板端标签后修改候选或排序规则 | 只可主张实验可执行性和无标签边界；不能主张 FPGA 正确、性能收益或搜索收益 |
| C3-H37 | 搜索成本已从“候选序号”扩展为五阶段墙钟与共享内存资源 | P7R137 保持 P7Q 720 条轨迹顺序/结论不变并对缺失成本 fail-open as `n/a`；P7R138 实测 Y00 24 身份公共 static/FSim 为 11.937/12.650 s，11 候选 cross-compile 合计约 1.70 s，并冻结 board runtime LOAD/STORE bytes、DMA calls、kernel invocations 的累计规则 | 完成 P7R132 板端池后要求 resource completeness=100%，并同时报告公共资格开销和各策略增量成本 | 可主张评价框架已完整；未上板前不能主张搜索墙钟或 DMA 已减少，逻辑 runtime 计数不能写成物理 AXI burst |
| C3-H38 | Y00 前瞻完整池确认了“共享内存流量可指导搜索”，但复杂特征无增益 | P7R144 严格沿用 P7R132 冻结的 11 个候选与顺序：11/11 通过三 seed FPGA 正确性，77/77 计时调用正确，完整池 oracle 为 17.621 ms。budget=1 的 oracle+2% 成功率：DMA-bytes/本文 lexicographic 100%，Random 15%，stock knob-XGB 5%，paper same-tile minimum-access 20%；但 bytes-only 与本文复杂排序在该池完全同序 | 再冻结一个独立几何并重复完整池协议；以 DMA bytes 为正式主规则，把 calls/padding/submission/command 作为消融或诊断 | 可主张一个未见 Y00 池上的前瞻搜索确认；不能主张跨几何泛化，也不能声称复杂特征优于 bytes-only，更不能写成击败 TopHub |
| C3-H39 | Y00 的输入驻留在五个同 tile 配对上方向一致 | P7R144 中五个具有 original 对照的 input-stationary 候选全部更快，改善分别为 18.29%、12.60%、7.97%、25.00%、28.80%；对应逻辑 LOAD 分别下降 26.73%、37.07%、43.45%、40.60%、13.52%，DMA calls 均下降 33.33% | 在另一几何做同样全 family 配对；必要时对 Y00 再做独立启动稳定性复测 | 可主张 Y00 池内 5/5 方向一致和 tile 依赖幅度；不能把中位或范围外推到整网或任意卷积 |
| C3-H40 | u-dma-buf 缓冲复用必须同时保持已资格化物理布局 | P7R139 以逐调用新分配完成 11/11 正确性，但计时第 3 轮在 192 MiB bump-allocation 路径耗尽；P7R141 改成复用但把顺序改为 data-weight-output 后仅 1/11 正确；P7R144 以 output-data-weight 原顺序复用同一组缓冲，恢复 11/11 正确和 77 个计时样本且无耗尽 | 增加同启动 A/B/A 分配顺序诊断或地址区间记录，定位是对齐、重叠还是硬件地址边界；在定位前把布局写入执行合同 | 可主张当前硬件/运行时下“复用”和“布局保持”都是正确性条件；三次运行尚非同启动随机化因果实验，不可直接宣称 u-dma-buf 驱动缺陷 |
| C3-H41 | Y02B00 权重驻留收益跨独立启动复现 | P7R145 在 boot `1d260256-086b-4bf5-a834-4092e773acea` 上 7/7 配对轮获胜，original 143.445 ms、barrier 109.657 ms，改善 23.55%；逻辑 LOAD/同步结构与 P7R123 相同。P7R123 的另一启动为 25.28% 改善、7/7 获胜 | 如需更强统计可报告两启动分层结果，不把 14 对样本伪装成独立 workload；第二个 prospective 搜索几何优先级更高 | 可主张该单 family 的跨启动方向与量级稳定；仍不能写 YOLO 平均、整网 FPS 或广泛权重驻留规律 |
| C3-H42 | Y03 证明手写容量公式不能替代真实编译合法性门 | P7R146 的完整 1280 ConfigEntity 域中有 185 个通过手写容量/复用谓词；按无标签哈希冻结的前八个 family 共 24 个模式却全部 lowering 失败。P7R148 只用 original lowering 合法性重冻结时，扫描 73 个 family 才获得 8 个 original-lowerable family，65 个因 padding、allocation 或 compact 2D DMA 约束失败，扫描耗时 57.321 s | 将 real lowering 作为候选资格的第一原则；手写容量只作早期粗筛。完整报告其公共墙钟，不伪装成零成本剪枝 | 可主张编译器落地约束显著收缩固定 FPGA 的名义搜索空间；不能把环境失败或模板实现限制说成硬件理论上不支持 |
| C3-H43 | Y03 第二个前瞻完整池确认 bytes-first 的局部有效性，但非最终规则 | P7R151 得到 13 个三 seed FSim-pass 身份；P7R152 在板端标签前冻结候选和规则；P7R154 中 13/13 通过三 seed FPGA 正确性、91/91 计时正确，oracle 为 11.528 ms。budget=1 的 oracle+2% 成功率为 DMA-bytes/复杂 lexicographic 100%、Random 0%、stock knob-XGB 15%、paper minimum-access 10% | Y04 已证明 bytes-only 不跨到 1×1；该条只保留为 Y03 局部正结果，正式规则改用共享内存服务代价 | 可主张第二个标签隔离池上的前瞻确认；不可再由 Y00/Y03 推导“静态 DMA 总字节优先”具有跨几何泛化 |
| C3-H44 | Y00/Y03 的两池汇总仍有效，但 bytes-only 泛化主张已撤回 | Y00+Y03 共 24 个 FPGA-correct 候选、40 个 replay seeds；budget=1 时 DMA-bytes 40/40 命中池 oracle+2%。aggregate budget=8 相对 stock knob-XGB 的逻辑 LOAD -42.13%、DMA calls -49.24%、五阶段墙钟 -15.34%，FPGA invocation +25% | 只作为两个 3×3 小池的历史结果；主方法和摘要必须同时报告 Y04 反例与后续服务代价修正 | 不得选择性省略 Y04 的 75.35% regret；不能把两池正结果写成通用规则 |
| C3-H45 | Cheng 2026 全文确认“低访存机制已覆盖，但 AutoTVM 搜索效率仍是空缺” | 9 页正式全文完整核对：论文定义四方案、TIR/runtime weight reuse、资源条件和三网络结果；Table 2 中 Scheme 4 在 c6/c9 慢于 Scheme 3。全文未给出 ConfigEntity 搜索空间、Random/XGB/SA 主实验、trial/sample budget、regret、time-to-target 或相同预算比较 | 将本文主贡献限定为驻留×tile 的跨配置搜索次序和 time-to-target；工程资格/部署只作支撑。进一步做在线成本感知多保真搜索可增强算法强度 | 可主张研究问题层面区别已由全文证实，当前两个离线完整池提供基础方法证据；不能把论文机制、硬件回退或本地 barrier 称原创，也不能把当前 replay 称成熟在线 tuner |
| C3-H46 | Y04 第三个标签隔离完整池否定 bytes-only 泛化 | 1×1 YOLOv3-tiny conv18 的 24/24 身份 static/FSim 通过；clean-start P7R165 中 24/24 FPGA-correct、168/168 timed call 正确、oracle 3.137311 ms。bytes-only/bytes-first budget=1 regret 分别 75.35%/37.96%；latency 与 calls/bytes 的 Spearman 约 0.9487/0.3745 | 正式模型必须保留请求启动与 submission；任何摘要都同时呈现该负结果 | 可主张总字节不是跨几何充分排序量；不能把单池相关性当因果常数，也不能删除 Y00/Y03 的局部正结果 |
| C3-H47 | 共享内存服务代价已完成开发冻结 | P7R166 在 P7Q+Y00/Y03/Y04 七个已暴露 workload 的 81 点系数网格中，按固定最小系数和规则选出 `B+65536*N_dma+131072*max(N_submit-1,0)`；开发集最大/平均首派发 regret 为 0.3855%/0.0662% | 系数冻结后在未见 latency 的独立池验证；系数只作 byte-equivalent acquisition penalty | 可主张开发得到一个物理可解释的服务代价代理；Y04 参与拟合，不能把七 workload 结果写成确认或物理 AXI 模型 |
| C3-H48 | 冻结 service proxy 获得一次 latency-unseen Y01 小池支持 | P7R169 冻结旧 P7R125 预注册、candidate timing sample=0 的 6 点池；P7R170 clean-start 后 3/6 FPGA-correct、21/21 timing 正确，oracle 13.675876 ms。service proxy 第 2 次 gross dispatch 命中 oracle；bytes-only/bytes-first 第 3 次，paper minimum-access 中位 4.5 次。相对 bytes-first，time-to-target 的已知墙钟 1.084/6.864 s、DMA calls 314/24666、FPGA invocations 8/25 | 扩大到更完整且真正在线逐候选的 latency-unseen ConfigSpace；将 validity 模型与 service acquisition 分工 | 可主张一次小型 latency-unseen recovery 支持和 time-to-target 降低；历史 correctness 已暴露、池仅 6 点，不能称全标签隔离或广泛泛化 |
| C3-H49 | clean start 已成为板端正确性合同的必要条件 | P7R163 在旧执行状态下同一 Y04 池 0/24；P7R164 重载相同 bitstream 后预注册 canary 3/3 正确；P7R165 clean-start 后全池 24/24 正确。P7R170 同流程又使旧 P7R125 的六点从 0/6 恢复到 3/6 | 后续完整池必须在任何健康/目标 tensor 分配前停止 RPC、重载冻结 bitstream、启动新 RPC并记录哈希；仍错误的候选才计 FPGA-invalid | 可主张执行状态污染会制造整池假阴性；当前数据未定位根因，不能武断归因 u-dma-buf 驱动或声称重载能修复所有候选 |
| C3-H50 | 真正按需付费的多保真搜索器已实现并完成开发消融 | candidate×phase 状态机只在执行 lowering/FSim/FPGA/measure 后揭示对应特征和标签。P7R174 的已暴露四池开发回放中，硬件多样 4→2 前沿相对全池资格的墙钟 184.589→80.394 s（-56.45%），lowering 78→26、FSim 65→8，保持同一目标质量和板端资源 | 需要在目标标签冻结前注册候选、策略源码和信息可见边界，并用完整池事后揭示 oracle | 可主张开发集上避免无必要的公共资格；不能把回放结果冒充未见验证，Model V 未稳定占优也必须保留 |
| C3-H51 | Y05 严格在线 holdout 支持固定 4→2 前沿，但暴露稀疏空间成本 | 24 个标签隔离身份最终为 18 lower-invalid、2 FSim-invalid、4 FPGA-correct；P7R177 在线动作链与冻结 replay 一致，第一次 FPGA/measure 身份是 16.305 ms 完整池 oracle。固定前沿一致回放相对全池资格墙钟 -20.48% | 首波收集器同时认证两个晋级候选，后续实现需做到候选级早停；稀疏空间应避免接近全池 lowering | 可主张首次严格部分可见搜索命中 oracle；实际收集器的额外候选工作不可计作在线早停节省，单一 holdout 不代表泛化 |
| C3-H52 | 生存率自适应多保真策略获得严格未见 Y06 支持 | 自适应阈值在 Y00--Y05 暴露开发集冻结。Y06 24 个身份中 22 lower-invalid、2 FSim/FPGA-correct；策略观察首 family 0/3 后走 sparse path，以 6 lower、1 FSim、1 compile、1 FPGA、1 measure 命中 30.103 ms 完整池 oracle。统一回放墙钟相对全池资格 -26.95%、相对固定 4→2 -29.16%，gross 24→6；首波与完整池独立计时为 30.074/30.103 ms | 增加更多新网络/几何、跨启动重复和在线预算停止；不能在 Y06 后修改阈值再把同池作为确认 | 可主张“共享内存服务代价 + 在线存活率反馈”在一个严格未见大 ConfigSpace 子池上减少 time-to-oracle；不能主张普适最优、物理 AXI 流量或整网 FPS |
| C3-H53 | 第二个未见 YOLO 池重复支持 sparse path | Y07 conv14 的 480 个 ConfigEntity 中 44 个过容量粗筛，冻结 24 身份后仅 2 个 lowering/FSim/FPGA-correct；策略以 6 lower、1 FSim、1 compile/FPGA/measure 命中 322.949 ms oracle，相对全池资格墙钟 -12.91%、相对固定 4→2 -14.42% | 最初误标 1×1 的 run01 在任何目标结果前废弃，正式证据只用正确描述的 3×3 run02；仍需稠密池 | 可主张 sparse 分支在第二个严格未见 YOLO 几何重复；两个最终候选不足以证明强性能排序泛化 |
| C3-H54 | ResNet50 十点正确池严格确认 dense 4→2 分支 | R50A 取自仓库 ResNet50 bottleneck stride-1 1×1 conv1，3456 个 ConfigEntity、510 个容量粗筛点、24 个冻结身份；首 family 2/3 存活后走 dense 分支。10 个 lowering/FSim 合法候选全部三 seed FPGA-correct、70/70 计时正确；第一次测量为 5.907 ms 完整池 oracle。相对全池资格墙钟 12.707→8.850 s（-30.35%），gross 24→10；pure family-wave 更慢且多测一点 | 需要 stage/整网传递与跨启动；首波收集器额外计时第二晋级点必须单列 | 可主张两条自适应分支都获得严格 holdout 支持并扩展到 ResNet50 真层；不可写成 ResNet50 整网 FPS 或 barrier 在 1×1 理论不可用 |
| C3-H55 | 三个严格自适应 holdout 汇总支持总体 time-to-oracle 降低 | Y06/Y07/R50A 共 72 个预注册身份，20-seed 配对汇总中自适应三池全部命中率 100%，中位 gross 22，对固定 4→2 的 58 和全池资格的 72；墙钟 19.735/22.426/25.983 s。自适应相对全池资格 gross -69.44%、墙钟 -24.05%，相对固定 4→2 墙钟 -12.00% | 继续增加模型/启动并报告置信区间；当前三个 target 均第一次命中会放大正向观感，必须保留完整池和失败候选 | 可主张规则冻结后三个留出上的总体搜索成本下降；FPGA invocations/目标 DMA 相同，不能伪称物理访存或板端调用减少 |
| C3-H56 | ResNet50 真层的输入驻留在五个 same-tile family 上方向一致 | R50A 完整 FPGA-correct 池形成五个 original/input-stationary 配对；input 模式 5/5 更快，改善 29.13%--79.08%、中位 62.80%，输入逻辑 DMA 字节中位 -75.00%、总 DMA 字节 -40.25%、DMA calls -72.73% | 跨 boot 或另一 ResNet50 层重复；当前精确双侧 sign test `p=0.0625`，bootstrap 只能作 family-cluster 描述 | 可主张该固定几何/boot 内完整 family 的机制一致性；五个相关 family 不是五个独立 workload，不能外推 ResNet50 整网或总体效应 |
| C3-H57 | 第二个 ResNet50 真层严格支持 adaptive sparse path | R50B 是 bottleneck conv3 的 1×1 CI128→CO512、28×28 扩张层；3456 点完整空间、581 点容量粗筛、24 个冻结身份中仅 4 个 lowering/FSim-pass，但 4/4 三 seed FPGA-correct、28/28 计时正确。首 family 0/3 后走 sparse path，以 6 lower/1 FSim/1 compile/FPGA/measure 命中 25.151 ms oracle；墙钟相对 exhaustive -52.36%、相对固定 4→2 -49.32% | 继续增加跨启动或 stage；不得用该池回调生存率阈值 | 可主张第四个严格留出及第三次 sparse 分支支持；完整性能池只有四点，不能宣称普适排序最优 |
| C3-H58 | R50B 给出与 R50A 不同的驻留适用边界 | R50B 的八个 input 身份均 lowering 失败；20 个 lower-invalid 分为 dma_2d_pattern 10、allocation_capacity 6、dma_compact_buffer 4。唯一 same-tile weight barrier 相对 original 为 25.746→25.151 ms（+2.31%），7/7 轮更快，总 DMA bytes -15.95%、calls -42.86%、instruction peak -94.43% | 新几何或跨 boot 复验这一较小收益；当前仅一对且同 boot | 可主张访问机制受 CI/CO 方向和真实 lowering 约束，且本对有小幅净收益；不能写成 ResNet50 普遍结论或统计显著总体效应 |
| C3-H59 | 四个严格自适应 holdout 汇总进一步支持 time-to-oracle 降低 | Y06/Y07/R50A/R50B 共 96 个预注册身份，20-seed 配对汇总中 adaptive 四池全部命中，median gross 28，对 fixed 82、exhaustive 96；墙钟 21.088/25.094/28.822 s。adaptive 相对 exhaustive gross -70.83%、wall -26.83%，相对 fixed wall -15.96% | 继续增加模型/boot；四池首测均为 oracle 的正向选择运气必须通过完整池和后续负结果约束 | 可主张规则冻结后四个 holdout 的总体资格成本下降；达到目标时 FPGA invocations 和逻辑 DMA 相同，不能冒充板端访问下降 |
| C3-H60 | exact residency dispatch 已进入普通 Relay/VTA 编译链 | `ExplicitResidencyDispatch` 绑定 workload+complete ConfigEntity，并在 `conv2d_packed.vta` schedule 入口 fail-closed 选择 mode；P7R227 原始/驻留保持 graph/params 相同但 TIR/二进制不同，P7R228 最小 Relay 板端 6/6 correctness、14/14 timing 正确，驻留 +2.36%、7/7 | 继续用另一 workload/mode 验证；生产 manifest 应绑定源码、bitstream 和 call-site | 可主张已资格化候选不再局限于独立测试模板；不能称通用 AutoTVM log 原生支持 residency，也不能绕过 correctness gate |
| C3-H61 | 预训练 ResNet50 完整 graph 首次验证驻留传递 | P7R231 两版 graph 相同、107 个参数语义逐字节一致；exact workload 对应四个算子实例。P7R232 三 seed 的 1000 个非零 logits A/B 全等，14/14 timing 配对全等；406.701→405.362 ms（+0.330%）、7/7，单次 weight LOAD -1,572,864 B、calls -1,728、submission +64 | 第二模型/启动；若需逐层不同模式，必须增加 Relay call-site 身份；另做正式 accuracy 时使用标准数据集 | 可主张完整 ResNet50 graph 的首次机制传递和端到端小幅收益；不能写成单层 dispatch、ImageNet accuracy、显著 FPS、物理 AXI 或跨网络普遍结论 |
| C3-H62 | YOLOv3-tiny 完整 graph 重复支持 same-tile input residency | P7R234/P7R235 将 Y00F06 exact pair 放回模型；三类输入、6/6 calls 的 8 个非零输出全等，14/14 timing 全等；266.250→264.247 ms（+0.758%）、7/7。单次 input LOAD -794,368 B，恰等于独立候选差值，整图总 DMA -1.73% | 第二 YOLO workload、跨 boot；正式检测质量需 COCO mAP | 可主张单个 YOLO `conv2` 的驻留机制收益完整传递；不能外推所有层、mAP、物理 AXI 或跨启动稳定性 |
| C3-H63 | 搜索所选 Y00 配置通过完整 YOLO stock TopHub 保护线 | P7R237/P7R238 的 selected input-stationary 相对无显式 route 的 stock TopHub 图为 267.503→264.220 ms（+1.243%）、7/7；非零输出全等，总逻辑 DMA -3.19%、calls -5.10% | 多 workload/boot；必须保留 TopHub 精确可用时无需重新搜索的适用前提 | 可主张当前一个 workload/boot 的 tile+residency 部署净收益；不能写“全面超过 TopHub”，且该 A/B 不能隔离纯 residency 因果 |
| C3-H64 | Y02 权重驻留机制传递到整图，但不能通过受保护 incumbent | P7R242/P7R243 固定 Y00 route，对 Y02 同一 ConfigEntity 比较 original/barrier：377.554→343.089 ms，+10.05%、6/6；单次 weight LOAD -10,616,832 B、calls -4,976，和孤立 pair 完全一致。但 Y02 由 TopHub 管理时整图为 265.090 ms，barrier 相对它慢 22.73% | 搜索/部署必须同时保留 same-tile 机制对与 incumbent 对照；不能用局部改善替代绝对质量 | 可主张权重驻留规律在第二个 YOLO workload 整图中仍有效；必须同时主张该具体 tile 不可部署，不能选择性省略负结果 |
| C3-H65 | 两条 YOLO route 的 DMA 改动可加，但最终部署需要边际准入 | P7R245--P7R249 四条 clean-start 因子边中，Y00 在 Y02 关闭/开启时分别 -3.413/-4.346 ms、均 7/7；Y02 在 Y00 关闭/开启时分别 +79.473/+78.355 ms、均 0/7。两种 latency interaction 估计约 -0.93/-1.12 ms，逻辑 DMA bytes/calls interaction 严格为 0 | P7R251/P7R252 已实现规划与清单；下一步增加三个以上 route 或跨 boot，不把两因子结果外推为全局最优 | 可主张多 workload route 没有破坏各自的逻辑访问语义，Y02 失败来自相对 incumbent 的绝对配置质量；当前只支持贪心边际门，不能声称解决任意组合搜索 |
| C3-H66 | 完整图替代版本并存存在真实 u-dma-buf 空间上界，且失败点已精确重建 | P7R259 的只读 allocator snapshot 表明每个 graph+params 占 34,300,928 B；四图 dry-load 为 137,203,712 B。首次运行先分配 32 MiB UOP 队列到 170,758,144 B，恰等于 P7R246 的失败水位，余量 30,568,448 B，小于随后 32 MiB instruction 请求 2,985,984 B；三图首跑后为 179,956,736 B | 测量器仍应流式加载或缩小命令 backing；allocator 是不释放的 bump 路径，不能把进程 high-water 当所有部署的同时存活峰值 | 可主张 P7R246 是四份 graph backing 与默认双 32 MiB 命令队列共同造成的确定性容量不足；不能归因 SD、候选数值错误、物理 AXI 或一般内核缺陷 |
| C3-H67 | incumbent-protected route composition 已实现为 fail-closed 规划器 | P7R250 合同把 Y00/Y02 按顺序绑定到 P7R249 与直接板端摘要哈希；P7R251 检查 current-baseline 连续性、单 route 增量、证据哈希/断言、输出一致、live-memory、7 轮全胜和负中位 delta。结果接受 Y00、拒绝 Y02，最终 route 集只含 Y00；3 项单测覆盖接受后拒绝、陈旧基线和证据篡改 | P7R252 已接入 exact graph manifest；下一步在三个以上 route/跨 boot 验证。当前 7/7 全胜门槛是保守部署策略，不冒充搜索理论最优 | 可主张多 workload 搜索结果具有可审计的部署准入闭环；不能称全局 route-subset optimizer，也不能把 post-search exposed evidence 当新 holdout |
| C3-H68 | route 规划结果已绑定 exact graph 部署清单 | P7R252 的 manifest `0ad7d0b7...11111b` 只保留 Y00，绑定完整 candidate identity、源合同、graph/params、binary `3fbecf15...55cb`、P7R241 板端摘要和 clean-start bitstream/u-dma-buf；Y02 以拒绝原因留档。2 项单测覆盖正常绑定与二进制篡改拒绝 | P7R261 已补齐该 exact graph 的命令容量证书；后续源码、图、参数、路由或硬件身份变化必须重新采峰并发证 | 可主张“搜索结果—边际准入—确切整图二进制”身份闭环；仍没有跨 boot manifest 或动态形状保证 |
| C3-H69 | u-dma-buf 分配路径和整图命令峰值已可只读审计 | P7R254 在隔离 runtime 暴露不触发分配的 allocator snapshot；P7R259 六个独立 clean-start 子会话重建 graph、首次运行和队列分配。stock/Y00/Y02/both 的 instruction peak 为 151,744/151,760/151,744/151,760 B，UOP peak 为 1,384/1,384/7,508/7,508 B；所有已运行变体输出相同 | 该 ABI 只用于隔离诊断 runtime；若 allocator 变为可释放/多线程，需扩展 alloc/free 与生命周期事件 | 可主张当前驱动的 requested、padding、allocation count 和 high-water；不能把逻辑 high-water 写成物理 AXI 流量或跨进程统一 live set |
| C3-H70 | 精确定容消除了四图测量器 OOM，并保持完整 2×2 因子实验 | P7R260 以四变体 allowlist 的 4 KiB 对齐容量 155,648 B instruction + 8,192 B UOP 重跑同一四 Executor clean-start 因子实验，12/12 correctness、32/32 timing 均正确，8 轮全部完成；stock/Y00/Y02/both 中位为 272.758/268.461/350.584/347.737 ms，方向与配对边一致 | 该容量覆盖四个已知静态 graph 变体，不是任意 graph 常数；P7R258 的 155,648+4,096 B 对 Y02 因 UOP peak 7,508 B 正确拒绝 | 可主张命令 backing 缩容使原先超出 192 MiB 的测量合同变为可执行，同时不改变既有性能结论；不能将四个 Executor 并存当真实在线部署需求 |
| C3-H71 | 最终选择的 exact YOLO graph 完成整图容量正例与少一页负例 | P7R261 将 P7R252 manifest ID、exact binary 与隔离 runtime 绑定；155,648 B instruction + 4,096 B UOP 下三类输入 3/3 输出集合逐哈希匹配历史结果，36 次 submission 的实测 peak 为 151,760/1,384 B。instruction 降一页到 151,552 B 时，首个合法 submission 后在下一段超限前拒绝，实测需求只高 208 B；默认 64 MiB 命令 backing 降为 159,744 B（-99.762%） | 单 boot、静态形状、replay disabled；源码、graph、参数、route、bitstream 或形状变化必须重新采峰，且尚无物理 AXI 计数 | 可主张最终 selected graph 的 exact command-capacity 部署合同和 fail-closed 边界；不能把 159,744 B 外推为 YOLO/VTA 通用容量或等同释放整个 192 MiB |
| C3-H72 | 同一 workload 的 ResNet50 实例已可按精确图节点隔离驻留 route | P7R262 排除了 TOPI 查询序号：4 个真实 Graph JSON 实例对应 7 次编译查询，不能作为 call-site ID。P7R264 run02 只把节点 57 的函数名改为 wrapper 别名，并绑定构建器/wrapper/8 个产物哈希；P7R265 run02 的 6/6 correctness、14/14 timing 输出全等，weight LOAD 每推理恰好 -393,216 B/-432 calls、同步 +16，等于单算子一份及四节点广播差值的四分之一 | 当前是精确 Graph JSON/二进制部署组合，节点号会随重新融合/编译变化；只测一个节点、一个 boot，且 406.375→406.017 ms 仅 +0.088%、4/7，不支持性能收益 | 可主张部署期 graph-node route isolation 和共享内存访问不泄漏；不能称 Relay/AutoTVM 原生 call-site tuner、显著整图加速、ImageNet accuracy 或物理 AXI 结果 |
| C3-H73 | 相同算子实例的逻辑 DMA 增量可加，但 latency 边际不可由 bytes 线性外推 | P7R267 的 k=1..4 每增加一点都严格为 weight LOAD -393,216 B/-432 calls、同步 +16；四个 latency 差却为 -0.036/-1.428/-1.314/-0.791 ms。P7R269 的八条上下文边具有完全相同 DMA 差，节点 98 在 full context 仍出现 +0.008 ms 边际 | 仅一个 R50B workload、一个 boot；逻辑 profiler 不是物理 AXI，极小 latency 差受噪声影响 | 可用 DMA 可加性形成候选组与审计访问身份，但最终接受必须相对当前 incumbent 走真实 FPGA 配对门；不能写 bytes 线性预测 latency |
| C3-H74 | 严格逐点 greedy 存在低信噪比 false negative，整组测试可恢复有效组合 | P7R271 四个单点中位均名义更快但只有 4/7、6/7、5/7、4/7，严格门全拒绝；同一次最终验证中 all-resident 相对 all-original -1.000 ms、9/9，且四点总 DMA 差严格为单点四倍 | 标签已暴露的开发反例；最终验证是三方 36 次执行，其中目标 A/B 两模式相关 24 次；未证明所有 workload 都应整组接受 | 可主张逐点部署门不具组合最优性，并据此设计 bundle action；不能称已有全局子集优化器或未见泛化 |
| C3-H75 | 完整 fused TIR 可修复孤立算子乘法模型遗漏的整图请求 | P7R273 因总 LOAD calls 预测 -192、板端 -204 而在计时前 fail closed；P7R274/P7R275 捕获 68+68 个最终模块，发现每实例另减 4 次零字节变化的 ACC LOAD，三实例聚合后的十项 LOAD/STORE 差与板端逐项相等 | 只核对 R50A 三处融合函数与现有 profiler 字段；Graph JSON/TIR/编译器改变需重发证 | 可主张整图共享内存预测必须以最终 fused lowering 和图出现次数为准；不能把孤立模板特征直接广播到融合图或冒充物理 AXI |
| C3-H76 | R50A input-stationary 获得第二种 ResNet50 整图正结果 | P7R276 在修复后的 prospective v2 合同下 6/6 correctness、14/14 timing 输出相同非零，十项 DMA 差均精确；三个实例合计 LOAD -1,204,224 B/-204 calls，337.273→330.125 ms（+2.165%）、7/7 | 单 workload、单 boot、预训练 logits 等价但未做 ImageNet accuracy；v2 预测修复来自 P7R273 已暴露失败 | 可主张融合 TIR 模型修复后通过新一轮 fail-closed 验证并有整图净收益；不能称独立 holdout、普遍 ResNet50 加速或正式精度结果 |
| C3-H77 | DMA 可加性引导的层次化 bundle admission 已有两个开发域支持 | P7R278 的 R50A 逐点与整组都选 `111`，但整组 20 次整网执行对比逐点 60 次，减少 66.67%，且 -6.977 ms、7/7；P7R279 与 R50B 汇总表明 bundle 在另一域还避免了逐点全拒绝的 false negative。方法先测合同兼容的整组，失败才递归拆分 | 两域标签均已暴露且同一 boot；整组失败后的递归分裂尚未实际执行，最坏测试数可能超过逐点法；仍是 Graph JSON/DSO 部署原型 | 可主张一个有明确反例、算法动作和板端成本结果的新开发方向；不能称未见确认、全局最优、原生 AutoTVM call-site 搜索或最坏成本占优 |
| C3-H78 | 失败 bundle 的递归拆分语义通过异构负向控制，且最坏成本边界被量化 | P7R280 哈希绑定既有 YOLO 四变体：强制 `Y00+Y02` 组为 +75.012 ms、0/8；拆分后 Y00 为 -4.196 ms、8/8，更新 incumbent 后 Y02 为 +79.285 ms、0/8，最终与 P7R251 一致只保留 Y00。层次路径用 3 次 pair test，逐点为 2 次，开销 +50% | 回顾性同 boot 数据；Y00/Y02 是不同 workload/mode，不属于正常 same-mode call-site 自动分组，只是对抗负控 | 可主张递归时必须相对更新 incumbent 且能从失败组合恢复安全子集，同时承认最坏成本可更高；不能冒充自然失败 bundle、在线执行或新 holdout |
| C3-H79 | 冻结的 group-first 规则获得首个新 tile 调用点/完整图 latency holdout 支持 | P7R281 在不使用目标完整图或 partial-mask latency 的条件下按注册表顺序冻结 R50AF00；P7R284 用最终 fused TIR 预注册三节点 LOAD -3,612,672 B/-9,072 calls；P7R285 冻结全部八个 mask。P7R286 四个独立 clean start 中三条 singleton 均 7/7 接受，逐点与整组都选 `111`；整组 422.292→374.452 ms（+12.776%）、7/7，只需 20 而非 60 次整网执行。全部输出非零且相等，DMA 预测逐项命中 | 与开发域共享 ResNet50、同一 workload 和 boot；历史算子 correctness/performance 已存在但未用于选择。不是新模型/工作负载留出，且仍没有正常 same-mode 自然失败 bundle | 可主张冻结规则在一个新 tile 的未知完整图/部分掩码 latency 上保持选择并降低 66.67% 验证执行；不能称跨 workload 泛化、最坏成本占优、全局最优或自然失败拆分已确认 |
| C3-H80 | 第二个新 tile holdout 复现 group-first 节省，并在完整图上证明 bytes-only 会错排 tile | P7R289--P7R295 按同一确定性规则冻结 R50AF01，fused TIR 预注册三节点 LOAD -8,429,568 B/-76,440 calls；四个 clean start 的 24 correctness+56 timing 输出全等且逐项命中。singleton/group 都选 `111`，group 898.821→435.519 ms（吞吐 +106.379%）、7/7，20 对 60 次执行。P7R296 对比 F00：F01 original bytes -23.25% 但 calls +622.22%、latency +112.84%；residency bytes -47.43% 但 calls +261.11%、latency +16.31% | 两个 tile 仍共享同一 workload、模型与 boot；大收益主要来自修复 F01 极端请求碎片，不能外推总体效应。P7R294 run01 是 askpass 环境失败，未接触候选；run02 才有效 | 可主张最终 fused-program bytes 与 calls 必须联合进入共享内存搜索特征，且 group-first 在第二 tile 仍节省 66.67% 验证执行；不能称已有通用 latency 模型、独立 workload 泛化、物理 AXI 或自然失败递归 |
| C3-H81 | R50C 独立 workload 严格确认生存率自适应多保真路径 | P7R297--P7R305 在任何 R50C latency 前冻结 24 个候选与既有服务代价；首 family 0/3 lowering-pass 后切换 `sparse_family_wave`，只考虑 6/24，执行 lower/FSim/compile/FPGA/measure=6/1/1/1/1。完整池为 11 static/FSim-pass、10 FPGA-correct，首个前瞻测量候选即 4.442324 ms oracle；可比 replay wall 822.451 对 exhaustive 8559.153 ms（-90.39%） | 单一 ResNet50 1x1 workload、单 boot；前瞻执行器另有 1713.486 ms 本地编排开销，必须与 canonical phase cost 分开；首测 oracle 不保证未来 workload 重复 | 可主张冻结的自适应规则在第五个、且相对 R50A/R50B 独立的新 workload 上保持 time-to-oracle 优势；不能主张普适最优、物理 AXI、整网收益或把编排开销隐藏 |
| C3-H82 | R50C 同时给出 input 驻留机制正证据与三 seed late-failure 反例 | P7R302/P7R307 的五个 FPGA-correct original/input same-tile 对 5/5 更快，改善 24.336%--79.853%、中位 44.694%，input bytes 中位 -75%、总 DMA calls 中位 -66.667%；唯一 lower/FSim-pass weight barrier 前两 seed 正确、第三 seed 17,920 元素错误并被拒绝 | 五对共享一个几何和 boot，符号检验 p=0.0625；weight 失败点无 latency；该晚失败使顺序 fail-fast 不节省本候选 seed 调用 | 可主张三 seed FPGA gate 对 FSim 后 late failure 的必要性，以及该几何 input 生命周期扩展的方向一致收益；不能把五对当五个 workload、声称 weight 机制普遍错误或每个失败候选都能 fail-fast 节省 |
| C3-H83 | 五个规则冻结后的严格多保真 holdout 汇总保持 100% pool-oracle 命中 | P7R306 汇总 Y06/Y07/R50A/R50B/R50C 共 120 个预注册身份、20 replay seeds：adaptive 中位 gross 34、wall 21910.022 ms，fixed 为 94/26580.804，exhaustive 为 120/37380.841；相对 exhaustive gross -71.667%、wall -41.387%，相对 fixed wall -17.572%，五池均命中 oracle | replay seeds 是确定性顺序消融而非独立开发板重复；三个池较稀疏，五个 workload 仍不足以给出通用最优保证；Model V 仍无稳定增益 | 可主张冻结的生存率分支在五个独立 workload 池上减少到达各自 oracle 的候选与墙钟；不能写成统计意义上的所有网络成功率 100% 或 ML 模型胜出 |
| C3-H84 | R50C 完成新的独立 workload 预训练 ResNet50 整图 latency 留出 | P7R308 在 R50C full-graph/partial-mask latency 前冻结 R50CF00 pair；P7R309--P7R311 从最终 fused TIR 确认节点 118/131/144/157/170 并预注册 LOAD -3,010,560 B/-270 calls。P7R312 的 6 correctness+14 timing 输出全等非零，十字段预测逐项命中，346.687→330.277 ms（+4.968%）、7/7；P7R313 验证 302 个链上文件与全部哈希/统计 | 同一模型和 boot、固定一个 tile；算子 correctness 已暴露但 operator latency 未用于选 pair；未做 ImageNet accuracy 或物理 AXI。P7R312 contract 的旧 R50A 说明由 P7R313 保留原文件并校正 | 可主张第三点首次在同一独立 workload 上贯通“严格算子搜索—机制解释—融合程序 DMA 预测—整图收益”；不能称跨模型、跨 boot、任意 call-site 组合或通用 ResNet50 加速率 |
| C3-H85 | 冻结服务系数加入最终 fused-program 高保真重排后修复两个 bytes-only 错选 | P7R314 在分析执行前绑定 P7R166 的 65536 B/request 旧系数和 P7R296 两 tile 证据；P7R315 不重拟合地比较 F00/F01。original/residency 中 bytes-only 分别产生 112.844%/16.308% regret、0/2 正确；fused service 均选 F00、2/2 正确。四个 exact program 的两两次序由 2/6 提高到 5/6 | F00/F01 标签在 P7R314 前已经暴露，故不是 prospective holdout；同 workload/model/boot 且没有 barrier extra-submission；仍错排 F00 original 与 F01 residency | 可主张将 final fusion/图出现次数作为少数晋级 tile 的额外搜索保真度有直接依据；不能称冻结公式是物理模型、能替代 FPGA latency、全面正确排序或已经跨 workload 前瞻确认 |
| C3-H86 | 固定 fused-service 标量在前瞻 R50C tile 冲突对上失败，支持 Pareto 冲突升级 | P7R316--P7R324 在整图 latency 未见且未用 operator latency 时冻结 F01/F02 input-stationary：38,097,920 B/980 calls 对 6,517,760 B/2,900 calls；旧 `B+65536N` 选 F01。P7R326 的 6 correctness+14 timing 全等非零且十字段 DMA 精确命中，但 375.140 对 350.550 ms，regret 7.015%、0/7。P7R327 审计并保留 P7R325 的 profiler 双执行归一化失败 | 同一 R50C workload/ResNet50/boot，仅两个新 tile；operator correctness 已暴露；结果否定固定系数泛化但不证明 Pareto 升级最优，也不是物理 AXI 服务模型 | 可主张 final fused TIR 应作为多目标高保真特征，联合支配点可静态筛而 bytes/calls 冲突点应升级测量；不能再主张 64 KiB/request 固定系数能跨 tile 可靠选优 |
| C3-H87 | final-fused Pareto 冲突升级规则已实现并完成事后成本审计 | P7R328 在四个重叠比较组中让 oracle 4/4 留在 `(bytes,calls)` 前沿；R50C 三 tile 因 F00 联合支配而从 3 缩为 1，另外三个冲突组均保留 2 点升级。bytes-only/service scalar 分别命中 2/4、3/4 | 标签均已暴露、比较组相互重叠，不是四个独立试验；冲突升级会保留更多 FPGA 测量，尚无新 workload 在线等预算确认；submission 轴在这些 input program 中相同 | 可主张可执行的 fail-safe 多目标搜索动作已经形成，并如实量化可靠性与测量成本的交换；不能主张 4/4 泛化、Pareto 支配必然蕴含更快或总能减少上板次数 |
| C3-H88 | final-fused 三轴 Pareto 获得新 workload 前瞻硬件缩减证据 | P7R329--P7R335 在未见 R50D 冻结 18 个 lowering/FSim 合法整图程序，按最终融合 `(bytes,calls,extra submissions)` 得到 4 点前沿；前瞻 4 点后再补测 14 点，15/18 FPGA-correct，前沿保留完整正确池 oracle、regret 0。相对穷举 candidate dispatch -77.78%、graph API -85.62%、driver invocations -86.59%、逻辑 DMA bytes -87.99%、calls -92.29% | 三轴特征需先支付所有候选 656.866 s 完整图构建，static/FSim 42.924/3.509 s 也是公共成本；bytes+fail-fast 达到 oracle+2% 同样只需 2 次；只覆盖一个新 ResNet50 workload/boot，3/5 weight barrier 在整图 FPGA 错误，全部正确候选仍慢于 stock | 可主张 Pareto 在一次严格前瞻留出中安全减少最后一级 FPGA 候选与共享内存流量；不能主张端到端调优已更快、全面优于 bytes-only、Pareto dominance 是 latency 定理、超过 TopHub 或 weight barrier 普遍正确 |
| C3-H89 | 廉价 operator-TIR Pareto 代理形成下一前瞻规则 | P7R336 在已暴露 R50D 上以算子 expanded bytes/calls/barrier indicator 得到 4 点前沿，与 exact fused 前沿重合 3 点，替换一个 FPGA-invalid weight 点为 FPGA-correct weight 点且保留 oracle；按历史实际耗时回放，stock+候选 full-graph build 656.866→169.592 s (-74.18%) | R50D 的 exact fused 与 FPGA 标签均已暴露，构建节省只是 replay；单 workload 不能证明代理前沿总能覆盖 exact/front oracle，barrier indicator 只是同步类别而非准确执行成本 | 可作为下一未见 workload 的预冻结规则；当前不能主张真实端到端构建成本已下降、代理可替代 final fused TIR/FPGA gate 或获得新 holdout 成功 |
| C3-H90 | 廉价 operator-TIR Pareto 代理在新 R50E 上前瞻保留 oracle 并取得实际构建与硬件成本节省 | P7R338--P7R348 在任何目标 full-graph/FPGA/latency 标签前冻结 R50E 24 身份和 P7R336 规则；12 个 lowering/FSim-pass 候选的代理前沿为 2 点，实际只构建 stock+2 点 100.120 s。前沿 2/2 正确；标签后补齐 10 点，完整池 12/12 正确且 oracle 是前沿首点。相对 stock+12 点实际构建总和 442.808 s，构建墙钟 -77.39%；加回 static/FSim 后选前本地墙钟 465.638→122.950 s (-73.60%)；candidate dispatch -83.33%、逻辑 DMA bytes -90.62%、calls -84.56% | 仅一个 ResNet50 stage3 1x1 workload、一个 boot、12 点池；全部候选比 stock 慢；logical DMA 不是物理 AXI；barrier indicator 不是准确同步模型 | 可主张 cheap semantic front 首次严格前瞻地同时减少 full-graph build 与 FPGA 搜索成本并保留 pool oracle；不能主张代理永不漏点、Pareto 支配蕴含更快、稳定优于 bytes-only、整网加速或跨模型泛化 |
| C3-H92 | Y08 冻结全池补充第三网络机制与 exact-oracle time-to-target 证据 | P7R354--P7R378 在任何 Y08 板端标签前冻结 YOLOv3-tiny conv12 六个合法候选及顺序；6/6 三 seed FPGA-correct、42/42 计时正确，三个 input same-tile 配对提升 3.70%--36.22%，bytes/calls/Pareto 首测命中 132.920 ms 精确 oracle | 只有一个冻结 Random seed，且它首点也进入 oracle+2%；策略成本是全池采集后的冻结前缀核算，不是五次独立在线执行；无 weight-barrier 合法点 | 可主张跨模型机制与精确 oracle 首测证据；不能主张 success@2% 优于 Random、完整在线墙钟优势或 Cheng weight 机制复现完成 |
| C3-H93 | R50C 预训练 ResNet50 全图收益获得跨启动方向和量级复现 | P7R312 与 P7R379 使用相同图、参数、A/B DSO 和 fused-TIR 合同；两 boot 分别为 346.687→330.277 ms（4.968%）和 346.912→330.290 ms（5.033%），合计 14/14 配对轮获胜；12 correctness+28 timing 输出全等非零，十项 DMA 差均精确 | 只有一个 frozen workload 的两个独立 boot，不能把轮次当独立启动；未做 ImageNet accuracy 或物理 AXI | 可主张该 R50CF00 pair 的整图驻留收益跨启动方向与量级稳定；不能外推为 ResNet50 通用加速率或跨 workload 统计结论 |
| C3-H94 | R50D 跨启动复验否定固定前沿必保精确 oracle，并定位 invalid dominator | 同一 18 点池两 boot 均 15/18 FPGA-correct；旧 boot 前沿保留 exact oracle，当前 boot 前沿最佳绝对 latency regret 0.0327%、paired-ratio regret 0.1706%，仍在 2% 带但 exact oracle 位于前沿外。该 oracle 只被一个 FPGA-invalid 前沿 weight 点支配；移除失败支配者后重算的唯一新增点就是 oracle | 第二启动标签已用于发现 peeling 规则；因此 peeling 只是 post-hoc 开发修正，尚未前瞻验证；全部候选仍慢于 stock | 必须把 H88 的 exact-oracle 结论限制为首 boot，并可主张 2/2 boot 保留 2% 等价质量；下一方法应前瞻验证 invalid-dominator peeling，不能再写静态 Pareto 保证精确 oracle |

| C3-H95 | ResNet50 holdout 的模型源几何一致性必须在冻结前自动核验 | P7R385 错把 `relay.testing.resnet` 的 stage2 unit1 conv2 写成 56x56/3x3/stride2；P7R388 在任何板端标签前用 InferType 确认真实值为 28x28/stride1，且命中历史 W02；P7R385--P7R387 被保留并撤销，冻结器新增源模型几何 fail-closed | P7R387 的合成 3x3/stride2 本地结果仍是可复现算子结果，但不能代表该 ResNet50 层；P7R393 又证明 Relay 推导目标不能路由到 MXNet 整图 | 可主张模型实现身份是 exact workload 搜索合同的一部分，并已补上自动检查；不能把被撤销池计作 holdout、ResNet50 结果或板端失败 |
| C3-H96 | invalid-dominator peeling 在新 R50G 获得首次前瞻近优成本结果，同时暴露 exact-oracle 边界 | P7R389--P7R398 对真实 `stage2_unit1_conv1` 在标签前冻结 5 点合法池、单点 wave0 和四种控制顺序；5/5 FPGA-correct。在线点首派发达到 oracle+2%，paired-ratio regret 0.714%；只构建/派发 1/5，候选 build/dispatch -80%，model-build 阶段墙钟 -68.60%。Random 达到 +2% 需3次，bytes/calls需1次；bytes/calls第2次找到 exact oracle，peeling/fixed-front不找精确 oracle | 本池 wave0 无 FPGA-invalid 点，所以 peeling 没有展开，与 fixed-front 等价；板端完整外层进程墙钟未写入原始结果；Relay-testing 模型为确定性随机参数，无 ImageNet accuracy；单 workload/boot/5点池 | 可主张 cheap proxy/peeling 的冻结停止规则在新池以较少构建和上板达到 2% 等价质量；不能主张 peeling fallback 已验证、Pareto 保证 exact oracle、全面优于 bytes/calls、完整 `T_search` 已降低或预训练整网精度/TopHub 优势 |
| C3-H97 | invalid-dominator peeling 在新 R50H 首次前瞻触发并恢复被错误支配点遮蔽的精确 oracle | P7R399--P7R408 对源模型一致的 `stage4_unit1_conv1` 在任何目标完整图/FPGA/latency 标签前冻结 15 点合法池、三点 wave0、支配关系和控制顺序。最低 DMA 的 R50HF05 weight-barrier 在 FPGA 第二 seed 输出错误；删除它后，唯一新增的 R50HF01 weight-barrier 通过并成为 14 点正确完整池 exact oracle。在线 4/15 dispatch、完整外层墙钟 312.942 s；相对穷举实测组件 candidate dispatch -73.33%、build action -76.12%、FPGA invocation -77.46%、逻辑 DMA bytes/calls -78.43%/-78.12% | 单 workload/boot，Relay-testing 随机参数不代表 ImageNet accuracy；控制是标签后冻结顺序 replay，不是独立在线进程；bytes-lazy 只需2次命中 exact，优于 peeling 的4次，故没有全面基线优势；P7R405 是漏传 askpass、板端接触前失败的空运行 | 可主张“算子代理支配点仿真通过但整图 FPGA 错误—fail-closed 删除—下一 Pareto 层恢复 oracle”的在线方法闭环与完整 `T_search`；不能主张 Pareto/peeling 总优于 bytes-only、跨 workload 稳定、物理 AXI、正式精度或 TopHub 优势 |
| C3-H98 | 第二个在线 R50I 留出保留 exact oracle，三留出汇总形成成本—质量与分支行为证据 | P7R409--P7R418 对源模型一致的 `stage3_unit1_conv1` 在标签前冻结 5 点合法池与两点 wave0；在线两点均通过并在 213.527 s 正常停止，补齐后 4/5 FPGA-correct，第二点为 exact oracle。相对五点穷举实测组件，dispatch/build action/FPGA invocation/逻辑 DMA bytes 分别 -60.00%/-63.41%/-51.22%/-52.22%。P7R419 合并 R50G/H/I：25 点池、23 点正确、在线 7 点（dispatch -72%），3/3 oracle+2%、2/3 exact、1/3 实际 expansion | 三个 workload 都来自一个 Relay-testing ResNet50 模型族和单个当前 boot；只有 R50H 含自然错误支配点并展开；R50I 的 calls-lazy 1 次优于 online 2 次，R50G exact regret 0.714%；P7R419 截止时控制仍是冻结顺序 replay，P7R424 仅补齐 R50I 的独立进程；随机参数不代表 ImageNet accuracy，R50G 无完整 outer wall | 可主张同一冻结方法在三个源模型一致留出中覆盖近优停止、错误反馈展开和精确停止三种轨迹，并以较少候选保持 3/3 的 2% 等价质量；不能主张第二次错误剥离、exact-oracle 保证、全面优于 bytes/calls/Random、跨模型泛化、物理 AXI 或正式精度 |
| C3-H99 | R50I 三条冻结基线已用独立完整进程完成同两候选预算对照 | P7R420--P7R424 严格读取 P7R412 标签前冻结的 calls/bytes/Random 顺序，各自重新执行模型准备、stock/candidate build、clean-start FPGA gate 与 timing，运行中不读 oracle。peeling/calls/bytes/Random 完整外层墙钟为 213.527/212.812/214.571/199.165 s；peeling 与 calls 命中 exact，calls 首次命中 146.179 s、peeling 为 213.311 s；bytes/Random 两点内均未进 oracle+2%。Random 第二点再次 FPGA-invalid，只有 8 correctness/14 timing，前三者各 12/28 | 控制顺序是前瞻冻结但独立执行发生在完整池标签之后；质量由结束后 immutable identity join 得到，不增加新 prospective holdout。固定派发数相同不等于 FPGA invocation 相同；Random 更短源于失败后跳过 timing。仅 R50I、同一 boot | 可主张 R50I 已有真实 complete-wall 的简单控制，不再仅依赖 replay；同时必须承认 calls-lazy 在该池以更少 time-to-oracle 胜过 peeling。不能主张本文方法普遍领先、Random 墙钟最短即质量最好或这些后补运行是新标签隔离验证 |
| C3-H100 | Y10 首次把 prospective cheap-proxy/peeling 与三条独立控制同步扩展到真实 YOLOv3-tiny 完整图 | P7R428--P7R443 在任何 Y10 板端标签前绑定 YOLO-320 cfg、权重和 Darknet 库，冻结 12 点池、三点 wave0、完整控制顺序与四条执行预算；12/12 候选三 seed、8 输出 FPGA-correct。peeling 以 3/12 命中 exact oracle，candidate build action/FPGA invocation/逻辑 DMA bytes/calls 较穷举分别 -76.26%/-75.00%/-75.06%/-75.08%；完整外层墙钟 212.727 s | 本池 0 个 FPGA-invalid 候选，peeling 未展开；calls 与单个 frozen Random 都在第1次 exact，peeling/bytes 到第3次；随机输入不代表 COCO mAP，单 boot，logical DMA 非物理 AXI。P7R433 是板前 target 适配失败，不计有效运行 | 可主张跨模型完整图、全输出等价、3/12 exact-oracle 与真实独立控制闭环；必须承认 calls 在 Y10 的 time-to-oracle 更优，不能主张方法普遍领先、第二次错误剥离、YOLO FPS/mAP 或跨 boot 稳定性 |
| C3-H101 | YOLO-320 同时给出合法性负边界和最少访存不等于最快的全图边界 | Y09 conv8 的 24 个预冻结身份只有 2 个 static/FSim-pass、0 个完整三模式 family，按合同不上板；Y10 四个完整 family 中 input-stationary 的算子 DMA 均下降且全图均略快 0.049%--1.424%，weight barrier 的算子 DMA bytes 下降 34.13%--92.92%，但全图三快一慢，Y10F02 慢 0.115% | Y09 只有本地合法性证据；Y10 一次仅替换完整图中的一个 conv18，收益被其余层稀释；barrier 不是 Cheng 源码级实现 | 可主张 tile/几何决定 lowering 可行性，DMA 下降需经全图 FPGA latency 裁决；不能把访存下降写成必然加速、把 Y09 写成板端失败，或声称精确复现 Cheng Scheme 4 |
| C3-H102 | 当前最终入选 Y10 整网已完成 exact allowlist 命令定容和不足一页 fail-closed | P7R444 绑定 P7R435 实际二进制与 P7R441 oracle，三输入 36 次提交实测 instruction/UOP 峰值 1,065,280/1,320 B；以页对齐 1,069,056+4,096 B 运行时三 seed 的全部 8 输出精确匹配。instruction 少一页后，前 5 次合法提交完成，下一条超限命令在 device submit 前拒绝；默认 RPC 和整图健康推理恢复 | 单静态图、单 boot、replay disabled；这是逻辑 queue backing，不是动态 shape、物理 AXI 或释放整块 192 MiB。旧 Y00 155,648+4,096 B 不可迁移 | 可主张最终部署身份按实测峰值将默认 64 MiB 命令 backing 减少 98.4009%，且不足容量安全拒绝；不能称 1,073,152 B 是 VTA 通用容量或带来 FPS 提升 |
| C3-H103 | 空历史原生 AutoTVM 与本文方法从 T0 调优到完整图 T1 的直接 A/B 尚缺失，已冻结补充协议 | P7R445 审计确认：既有 `stock-XGB` 主证据是完整池顺序 replay；P7R435 的 212.727 s 不含候选生成、static lowering 和 FSim；Y10 穷举的 build/FPGA 墙钟是分项实测求和。因此冻结共同 T0/T1、空 history、同图三输入全输出正确性、七轮平衡计时、三 clean-start session，以及 60/120/300/600 s anytime 检查点 | 协议尚未实现和上板，表中从零成本及最终 A/B 整图 latency 均不得预填；两边搜索空间不同的主比较只能归因于系统流程，还需同空间消融归因搜索器 | 完成前只能主张池内、阶段级及本文在线执行段节省，不能写“相对从头 AutoTVM 端到端调优时间下降 X%”；完成后以 `T1-T0`、time-to-best+2%、FPGA invocation 和最终整图 latency 为主结论 |
| C3-H104 | Y10 空历史 AutoTVM-XGB 与本文方法的三对三 T0→T1 系统流程 A/B 已完成 | P7R452/P7R456/P7R457 的 XGB 三种子从空 history 在同一 1280 点原始空间提出 173/173/157 点，均选 config543；P7R453/P7R455/P7R458 逐行重演 P7R428 身份而不读取旧 latency，24 个正式身份中 12 个 lowering/FSim-pass、只派发 3 个完整图。P7R460 汇总中位 T0→T1 为 696.427 对 244.844 s（-64.84%，2.84×），本文 120.279 s 即获得 XGB 最终图 +2% 内的可部署图；最终整图为 1054.773 对 1057.774 ms（本文慢 0.285%）。六轮均三输入/八输出正确、7/7 配对获胜 | Y10 已暴露，本文是身份精确成本重演而非新 prospective holdout；两边搜索空间与测量粒度不同，未做同一进程三方交错或相同空间在线消融。XGB 采用官方搜索器/原始空间，但为隔离 P7R447 的 fatal RPC 污染使用三 seed 和逐候选 clean-start 安全 runner；本文完整图安全门每轮逻辑 DMA约 14.486 GB，高于 XGB 可审计下界 5.052 GB | 可在 Y10 case study 中主张安全完整系统将从零调优到可部署整图的中位墙钟降低 64.84%，并以 0.285% latency 差保持 XGB 的 2% 等价质量；不能把全部节省归因于搜索算法、默认 AutoTVM runner、声称所有资源下降、跨网络泛化或 COCO mAP |
| C3-H105 | Y10 相同候选空间和完整图执行路径上的 DMA prior 与 mode-aware XGB 三对三归因消融已完成 | P7R462--P7R467 各自固定 12 点池、budget=6、三输入八输出正确性与七轮配对计时；36/36 dispatch 正确。P7R468 汇总显示两者均首派发进入 oracle+2%；DMA prior 三次均第3次 exact，XGB 为第4/6/6次，exact time-to-quality 中位 212.564 对 396.797 s（-46.43%，1.87× 提前） | Y10 标签在 P7R461 前已经存在，选择器虽不读旧标签，仍只是独立成本重演而非 prospective holdout；两边执行满六点后总墙钟中位 400.933 对 398.963 s，不能声称固定终止预算总成本下降；logical DMA 非物理 AXI | 可主张在已暴露 Y10 的相同空间、预算和执行路径上，DMA 先验更早找到 exact oracle，并据此把搜索次序收益与 P7R460 系统流程收益分开；不能主张 +2% 首次命中优势、跨网络泛化或把 P7R460 的 64.84% 全归因于搜索器 |
| C3-H106 | HW-Aware、ML²Tuner P/V/A 与 Cheng 四方案的统一实验实现及三个 ResNet18 候选协议已冻结 | P7R469 绑定六策略、Rieber 四级消融、ML²Tuner `N=10, alpha=1`、20 seeds、W0/T0/T1 与失败计费；P7R470 实际枚举 H1/H2/H3 的 2304/1600/480 点完整 original 域，并各冻结 24 tile×4 mode。新增 Scheme 4 在三个历史配置上同时达到 input/weight 低访存并通过 3×3 seed FSim；相关测试 62/62 通过 | 计划中的“每个完整域都是1280”被真实 ConfigSpace 否定；目标 288 身份尚未 lowering/FSim/交叉编译/上板，故没有 valid yield、模型指标、搜索成本或新性能结论。组合模式是功能级实现，不是 Cheng 源码级复现 | 可主张文献对齐方法、信息隔离、资源 fallback 和候选冻结已完成；不能主张已复现论文性能、任何基线已被改进、R18 算子/整图加速、跨网络泛化或物理 AXI 流量 |

## 创新等级

P7R349 / C3-H91 补充 H90：同目标成本回放发现原前沿预构建成本 134.684 s 高于简单 calls 按需
构建的 99.155 s；反转前沿内部哈希顺序需 151.012 s，与 bytes 按需构建持平。1000 随机排列中位
385.332 s。上述均为已计时阶段之和与事后消融，未覆盖完整进程墙钟。保留 H90 的前沿筛选与构建
节省结论；不能将首点命中当作 Pareto 排序能力，也不能用穷举分母声称优于所有搜索基线。
下一验证方向是冻结预算下逐点构建，以及完整外层计时。它不增加独立 workload/holdout 数。

- CCF-C 闭环：至少两个可计时几何的 same-tile 机制结果、一个完整 FPGA-correct 候选池，以及
  20-seed 等 gross-budget 搜索比较，证明本文方法以更少测量达到与池 oracle 相同的质量；命令容量
  通过/不足容量 fail-closed 是支撑实验，不替代搜索主结果。
- 当前：已有较强硕士论文系统型创新闭环，并形成 CCF-C 风格 case-study 的实验骨架。H45 依据全文
  确认 Cheng 2026 已覆盖低访存机制与资源回退，但没有解决跨 ConfigEntity 的等预算搜索效率；
  H38/H43 是两个 3×3 标签隔离池的 bytes-only 局部正结果，H46 的第三个 1×1 标签隔离池随后给出
  必须保留的反例。H47 将方法修正并冻结为“编译/仿真合法性门 + 字节/请求/提交服务代价 + FPGA
  correctness”，H48 在未见 latency 的六点池中支持其 time-to-target 优势，H49 则把 clean start
  纳入执行合同。H50--H59 已进一步实现逐阶段按需付费的在线多保真调度；H81/H83 将严格审计扩展到
  Y06/Y07/R50A/R50B/R50C 五个未见 workload。sparse/dense 两条分支均获支持，五池汇总相对全池
  资格 gross -71.67%、墙钟 -41.39%。R50A 提供跨到 ResNet50 的 10 点正确池，弥补两个 YOLO 稀疏池各仅两点的
  弱点；H56/H58 进一步以 same-tile 配对把搜索结果和输入/权重驻留的 DMA/latency 机制相连。H60/H61
  已把同一资格化 R50B 候选从独立模板送入普通 Relay 和预训练 ResNet50 完整 graph，补上首次整图
  传递；H62/H63 又用 YOLOv3-tiny 建立第二网络的 same-tile 因果对照和 stock TopHub 保护对照。
  H64/H65 进一步完成第二个 YOLO workload 与两 route 的 2×2 边际审计：驻留机制本身有效，但局部
  正候选仍可能输给受保护 incumbent，逻辑 DMA 改动虽可加，最终 route 必须按图上下文做边际准入。
  H66/H69 已把四份完整图并存耗尽 192 MiB 的原因精确分解到 graph backing 与默认命令队列；H67 已将边际准入实现为哈希绑定、
  基线连续性检查的 fail-closed 规划器，并正确接受 Y00、拒绝 Y02；H68 再把该结果绑定到实际执行过
  的 exact graph 二进制；H70 证明四变体 allowlist 定容可消除测量器 OOM，H71 又对最终 Y00 exact
  graph 完成 155,648+4,096 B 正向运行和少一页负向拒绝。H72 又把 ResNet50 route 从同 workload
  四节点广播收窄到精确 Graph JSON 节点，真实 FPGA 的 DMA 差值严格隔离为一份单算子变化；当前
  仍是部署期组合而非原生 call-site 搜索。H73--H78 据此建立 DMA 可加性引导的层次化 bundle
  admission，并保留低信噪比 singleton false negative 与失败递归额外成本；H79/H80 再以
  R50AF00/F01 两个新 tile 的调用点/完整图 latency holdout 确认同样选择可将整网验证执行从 60
  降到 20。H80 同时给出完整图 bytes-only 排序反例：F01 搬运更少但请求碎片更多、最终更慢，直接
  支持共享内存服务代价联合使用 bytes/calls。两个留出都没有改变网络、workload 或 boot；H82--H84
  随后以独立 R50C workload 串起严格算子搜索、五组 same-tile input 机制、late-seed FPGA 反例和
  五调用点整图验证，融合 TIR 的 LOAD `-3,010,560 B/-270 calls` 精确命中，整图提升 4.968%、7/7。
  H85 再将 final fusion 变成候选搜索的一种高保真动作：旧系数无重拟合修复两个 bytes-only 错选；
  H86 的前瞻 R50C 冲突对随后以 7.015% regret 否定固定标量的跨 tile 泛化。正式方法因此收敛为
  final-fused 多目标联合支配与冲突升级，而不是寻找一个万能 request 系数。H87 已实现该动作，
  在重叠事后组中 4/4 保留 oracle；H88 又在新 R50D 的严格前瞻池中用 4/18 个候选保留完整正确池
  oracle，并显著减少最后一级 FPGA 调用和逻辑 DMA。H88 后的主要缺口转为消除 656.866 s 全候选
  整图构建公共成本。H89 在暴露的 R50D 上开发廉价算子代理，H90
  随后在新 R50E 中真正按先筛后构建执行：2/12 前沿保留完整正确池 oracle，实际模型构建墙钟
  -77.39%、候选派发 -83.33%。这关闭了“只能节省最后一级 FPGA、不能节省全候选整图构建”的主要
  缺口；剩余边界转为代理只有一个独立前瞻 workload、全部 R50E 候选慢于 stock，以及跨模型/跨 boot
  外部有效性。
  命令 allowlist 容量的正向与不足一页 fail-closed 已从单算子推进到完整图。主要方法闭环现在成立，剩余工作转为
  原生 call-site 搜索、跨启动和三个以上 route 的组合验证；当前仍不能保证正式 CCF-C 录用水平。
- 工程案例：若最终只有 Y02B00 单 family 正结果，则可作为扎实硕士论文案例，但论文级外部有效性不足。

## 永久禁止的表述

- “首次提出输入优先/权重驻留”；
- “逻辑 DMA calls 就是 AXI burst 数”；
- “单 boot 的 repeat/帧是独立样本”；
- “旧 5312/2792 B 峰值证明所有新 schedule 都可用 T2656”；
- “u-dma-buf 本身是本文创新”；
- “DMA 总字节最少就一定最快，且已经跨卷积泛化”；
- “固定 64 KiB/request 的服务分数能够跨 tile 可靠预测最快程序”；
- “2025 年开始课题，因此无需引用 2026 正式论文”。
- “P7R config455 的同 tile 加速已经打赢 TopHub、提升 stage 或提高整网 FPS”。
- “一个候选相对 same-tile original 更快，因此可以无条件叠加到整网部署”。
