# P7R376：当前恢复断点与未完成项

当前状态：板端连接阻塞，目标未完成。最后复查SSH仍为No route to host；本机
路由为192.168.1.247 dev eth1 src 192.168.1.100，邻居FAILED。
未读取到当前boot、SD或FPGA状态，不据此声称SD再次损坏。

## 已完成的离线增量

- 驻留生命周期weight/ACC/input容量规则与单元测试，独立于正式搜索器。
- 12个历史池288身份回放；结果不能作为未见验证。
- 两个已有几何的额外候选池各48点，规则/选点在新编译前冻结。
- 普通tile容量与驻留容量实测消融：Y08 lowering 24→14，R50B 45→40，
  各自成功身份及TIR集合不变。小样本主机计时，不是完整AutoTune加速。
- 额外Y08池8个lower-pass中6个三seed FSim-pass；额外R50B池19/19
  三seed FSim-pass。两池不能合并成一个新独立网络证据。
- RF有效性代理首4合法点4→4，保留无增益结论；未完成论文ML2Tuner原样复现。

## 必须继续而不能用离线计数替代的工作

1. 原Y08正式池P7R354–357仍是6个候选，未采集FPGA/latency标签。新增
   P7R368/P7R373容量池绝不替代或混入它。
2. 连接恢复后读取当前boot与文件系统日志，在RAM恢复已核对hash的runtime、
   u-dma-buf和bitstream，然后必要的健康canary。健康检查不是重做历史研究。
3. 按冻结协议进行原池正确性与性能测量；现有ResNet50执行适配器不能未经
   YOLO多输出/输入尺寸适配就用于该任务。完整预算必须包含所有实际阶段。
4. 进一步判断共享内存先验能否增强有效性/性能搜索：当前编译过滤的局部
   时间收益不能替代达到同等性能目标的端到端成本。Rieber稀疏降级复现、
   RF代理与完整论文方法的区别仍须保留。

当前没有待轮询的本轮实验进程：P7R374编译和P7R375仿真均已终止成功。
停止重复离线回放以代替关键板端证据。需要用户通过串口恢复目标IP/网线连接；
恢复后先复用不可变产物，不重新生成正式候选，不把旧标签当新标签。
